문서 상태: 작성완료

# REV-TKT-140-r1-draft — 스플래시 재방문 단축 + 패널 면 제거 (리뷰어 초안, 판정은 PM)

> 작성: 리뷰어(Claude Sonnet 5), 2026-09-15. **이 문서는 초안이다 — finished 전환·커밋은 PM 몫.**
> 대상: TKT-140(localStorage 재방문 플래그, 패널 배경/테두리 제거).

## 요약 권고: **통과 (블로커 0)**

## 실행 검증

- `pwd`/branch(`codex/v0.7.0-tone`) 확인 후 `frontend/`에서 실행.
- 메인 Node unit 21/21, root/Pages-base build 각 49 modules — TKT-138/139와 동일 스위트로 재확인, 그린.
- `splashTone.e2e.mjs`(Pages-base 자체 호스팅) — **6/6 통과**: 첫 방문 10초·ISO 저장, 재방문 3초, 재생 후 10초 전체 흐름, 만료·저장불가 fallback, reduced-motion, 파비콘 회귀.
  - 최초 시도에서 다른 e2e 프로세스와 동시 실행돼 6/6 전실패(180초, 리소스 경합으로 판단)했으나, **격리 재실행에서 6/6 통과**를 재확인해 코드 결함이 아님을 검증했다.
- **라이브 확인**(같은 Pages-base 프리뷰, 375px): 실제로 `localStorage.getItem('splash:seen')`에 ISO 타임스탬프가 있는 상태로 재방문하니 `DOORS OPENING` 한 문구 + "3초 후 자동 전환" 안내만 표시됨 — 재방문 단축 동작을 실측으로 확인. `다시 재생` 케이스는 E2E가 이미 커버하므로 중복 조작하지 않았다.

## 완료 조건 대조

1. **첫 방문 10초 → 플래그 → 재방문 3초 → 다시 재생 10초, 3경로** — E2E 3경로 모두 통과, 재방문 경로는 라이브로도 재현.
2. **패널 배경/테두리 computed 투명(375/1440), 기존 splash E2E 3/3 유지** — E2E 6/6 안에 포함(기존 3개+신규 3개), computed-style 단언이 테스트 코드에 존재함을 확인.
3. **build·unit 그린** — 위 실행 검증대로 확인.

## 상태 제안 (판정은 PM)

블로커 0. **finished 전환 권장.**
