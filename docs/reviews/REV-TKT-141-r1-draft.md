문서 상태: 작성완료

# REV-TKT-141-r1-draft — 정차역 편집 시트 기본 3칸 + 더 적기 접힘 (리뷰어 초안, 판정은 PM)

> 작성: 리뷰어(Claude Sonnet 5), 2026-09-15. **이 문서는 초안이다 — finished 전환·커밋은 PM 몫.**
> 대상: TKT-141([PM 의문] 4 승인분).

## 요약 권고: **통과 (블로커 0) — 부가로 이 티켓이 세션 전체의 날짜 종속 테스트 결함을 해소함**

## 실행 검증

- `pwd`/branch(`codex/v0.7.0-tone`) 확인 후 `frontend/`에서 실행.
- 메인 Node unit 22/22, root/Pages-base build 각 49 modules 통과.
- `VoyageRouteMap.e2e.mjs`(Pages-base 자체 호스팅) — **4/4 통과**(기존 2 + 신규 시트 압축 2).

## [확인] 직전 REV-TKT-138-r1-draft 에서 지적한 날짜 종속 테스트가 이 티켓에서 해소됨

TKT-138 검증 때 `VoyageRouteMap.e2e.mjs`가 `findTripDayIndex()`의 기본 `new Date()` 때문에 "740km" 하드코딩이 실제 달력 날짜(진행 중인 2026-09-08~18 여행)를 따라 깨진다고 지적했다. 이 티켓의 "검증" 절이 "테스트 날짜는 제품 일정과 무관하게 2026-09-14로 고정해 기존 740km 단언의 날짜 민감성도 제거했다"고 자체 기술했는데, **직접 재현해 사실로 확인했다** — 오늘(2026-09-15) 재실행에서도 4/4 그린. `git diff`로 `VoyageRouteMap.e2e.mjs`에 시계 고정 관련 로직이 추가된 것도 확인했다(순수 코드 추적, 실행 결과로 최종 검증).

## 완료 조건 대조

1. **375에서 시트 첫 화면 3칸+저장 버튼 스크롤 없이** — E2E 신규 서브테스트에 포함, 코드상 `scrollTop=0` 단언 확인.
2. **값 있는 정차역 자동 펼침, 117 E2E 그린** — 같은 4/4 안에서 확인.

## [참고] 배치 전체에 대한 관찰 — TKT-138 블로커 재확인 필요

같은 작업 트리에서 TKT-144까지 반영된 현재 상태로 `WritingStudio.e2e.mjs`를 다시 돌려보니 **9/9 전부 통과**했다 — 즉 REV-TKT-138-r1-draft가 지적한 `.writer-bar` 375px 2px overflow가 지금은 재현되지 않는다(`.writer-bar` 자체 버튼 폭은 그대로인데 상위 컨테이너 쪽에서 2px 여유가 더 생김 — 어느 후속 커밋이 원인인지는 특정하지 않았다). TKT-141 자신의 diff는 `WritingStudio.vue`를 건드리지 않으므로 이 티켓의 공로는 아니지만, **PM이 TKT-138을 판정할 때는 최신 트리 기준으로 한 번 더 확인**하는 것을 권장한다 — 이미 해소됐다면 반려 없이 통과 처리가 맞다.

## 상태 제안 (판정은 PM)

블로커 0. **finished 전환 권장.**
