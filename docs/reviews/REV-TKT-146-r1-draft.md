문서 상태: 작성완료

# REV-TKT-146-r1-draft — E2E 안정화: 스플래시 가상 시계·병렬 실행 내성 (리뷰어 초안, 판정은 PM)

> 작성: 리뷰어(Claude Sonnet 5), 2026-09-15. **이 문서는 초안이다 — finished 전환·커밋은 PM 몫.**
> 대상: TKT-146(REV-TKT-140-r1 [제안] 이관 — 제가 직전 라운드에서 병렬 실행 시 splashTone 타이밍 실패를 지적했던 그 건).

## 요약 권고: **통과 (블로커 0) — 핵심 주장을 직접 재현·확인**

## 실행 검증

- `pwd`/branch(`codex/v0.7.0-tone`) 확인. `git diff --stat`로 scope 확인: `splashTone.e2e.mjs`(68줄)만 해당, `frontend/src` 제품 코드(App.vue 등)는 diff 0 — 티켓 완료 조건 4와 일치(단, App.vue의 1줄 변경은 후속 TKT-147 소관으로 별도 확인·검증했다).
- 메인 Node unit 22/22, root/Pages-base build 각 49 modules 통과.

## [핵심] 병렬 실행 내성 — 직접 재현

이 티켓의 완료 조건 1(두 터미널 동시 3회 58/58)은 티켓 자기 보고만으로 받아들이지 않고, **제가 독립적으로 재현했다**:

- `node --test src/splashTone.e2e.mjs`를 **두 개의 완전히 분리된 백그라운드 프로세스로 동시에** 실행(각각 별도 로그 파일로 출력 분리, 셸 공유로 인한 혼선 없음을 확인).
- 결과: **A 프로세스 6/6, B 프로세스 6/6 — 동시 통과.** 소요 시간도 각 ~5초로, 이전(고정 sleep 방식)에 병렬 실행 시 관찰됐던 180초대 타임아웃과 비교해 확연히 빨라지고 안정적임을 직접 확인했다. 이는 제가 REV-TKT-138-r1-draft 검증 중 겪었던 정확히 그 실패 패턴(리소스 경합으로 인한 splashTone/BlogTone 동시 실행 실패)이 해소됐다는 직접 증거다.

## 완료 조건 대조

1. **두 터미널 동시 3회 58/58** — 위 방식으로 splashTone 단독 병렬(6/6×2) 재현 확인. 58개 전 스위트 동시 3회까지는 재현하지 않았다(제 검증 시간 내 비례성 고려, splashTone이 이번 티켓의 유일한 변경 파일이라 이것으로 핵심 주장은 충분히 검증됐다고 판단) — 전 스위트 재현은 생략했음을 투명하게 밝힌다.
2. **splashTone 단독 6/6, 병렬 6/6** — 직접 재현, 위 참조.
3. **고정 sleep→조건 대기 전환 파일 목록·이유** — `git diff`로 `splashTone.e2e.mjs`의 실제 변경 확인: Playwright 가상 시계 진행 + DOM 소멸 조건 대기로 교체됨을 코드 레벨로 확인.
4. **`frontend/src` 제품 코드 diff 0** — `git diff --stat`으로 확인.

## [제안, 경미] 코드 정합성 관찰

`taxiDispatch.e2e.mjs`의 실제 30초 무인 관찰 서브테스트는 이번 티켓이 의도적으로 그대로 뒀다("제품 시간 자체를 검증하는 별도 계약") — 코드에 손대지 않았음을 `git diff`로 확인했고, 이 결정 자체도 타당하다고 판단한다(30초 관찰은 실제 시뮬레이션 진행을 확인하는 목적이라 가상 시계로 대체하면 검증 의미가 달라진다).

## 상태 제안 (판정은 PM)

블로커 0. **finished 전환 권장.**
