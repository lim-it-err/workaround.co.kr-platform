문서 상태: 작성완료

# UX-TKT-102-r15 — 격납고 목업 r2 변경분

- 작성: codex-8 · 2026-09-14 · **UX 초안, 최종 판정은 PM**.
- 대상은 `frontend/public/mockups/simhub.html` **tone r2**이며 보고서 순번만 r15다. PM이 TKT-114 r1 반려와 함께 수정한 버전이다. TKT-114 r2 실화면은 이미 finished이며 이번 목업 검수로 그 판정을 바꾸지 않는다.
- 원본 SHA-256 `d74fa61f21f973905a672fafa4ac26907fcb0daa982aec506fd9aacf7830f0d4`, snapshot/dist/제출 시 원본 일치. [환경·비교](UX-TKT-102-r15-assets/manifest.json), [현재 원본](UX-TKT-102-r15-assets/source/simhub-r2.html), [r1 원본](UX-TKT-102-r15-assets/source/simhub-r1.html).

## 변경 대조

기존 가공 추천 시나리오와 ‘사라진 수하물’을 걷고, **멈춘 엘리베이터를 주인공으로·심야 택시를 한 행으로** 남겼다. 버튼 수 4→2, ‘미션 시작/다시하기’에서 ‘시작’으로 통일됐다. 가짜 콘텐츠를 줄이고 선택을 분명하게 만든 개선이다. CSS는 r1과 바이트 단위로 동일하며 하단 운영 메모는 r2에서 새로 추가됐다.

## 발견과 의견

| 항목 | 파일:라인과 실렌더 근거 | 디자이너 의견 |
|---|---|---|
| **B1 [블로커] 운영 메모가 사용자 본문으로 들어옴 — 신규** | `frontend/public/mockups/simhub.html:1`의 `main > p.tag`: ‘화이트채플 합류(TKT-107) 시 히어로 교체 · 딥링크 /elevator /taxi 유지 (D-012)’. 375에서 y484.34부터 두 줄, 1440에서는 y502.34에 노출된다. [375](UX-TKT-102-r15-assets/simhub-375-dark.png), [1440](UX-TKT-102-r15-assets/simhub-1440-dark.png). | 카피 원칙 `design/ux-copy-audit-2026-08-16.md:22`의 내부 티켓 번호 금지에 어긋난다. 상단 MOCKUP 식별 배너와 달리 main 안의 제품 카피로 배치돼 이식 기준을 혼동시킨다. 운영 설명은 티켓/리뷰 또는 HTML 주석에 두고 사용자 본문에서는 제외하도록 권고한다. **실제 앱에 이 문구가 노출된다는 판정은 아니다.** |
| **I1 [중요] 시작 버튼 높이 38px — 기존 CSS 잔존** | 같은 파일 `:1`의 `button{padding:10px 13px;…}`. 두 버튼 모두 모든 6환경에서 **51.06×38px**, 40px 하한 미달. [390](UX-TKT-102-r15-assets/simhub-390-dark.png), [측정값](UX-TKT-102-r15-assets/results.json). | 실제 버튼에 min-height ≥40px를 확보한다. r2에서 새로 생긴 CSS 회귀로 보지 않으며, 다음 목업 수정 시 해소할 잔존 항목이다. |
| **S1 [제안] 동일한 ‘시작’ 두 개의 목적지 식별** | 같은 파일 `:1`의 `.hero > button`·`.row > button`. 시각적으로 가까운 제목은 있지만 접근 가능한 이름은 둘 다 ‘시작’, aria-label 없음. | 승인된 가시 문구 ‘시작’은 유지하면서 ‘멈춘 엘리베이터 시작’/‘심야 택시 시작’처럼 제목을 포함한 접근 가능한 이름을 주는 편이 목적지를 구분하기 쉽다. 스크린리더 실기 검증이나 전체 접근성 판정은 수행하지 않았다. |

원본 HTML은 실제로 한 줄이므로 위 파일:라인은 모두 1이다. CSS 선택자와 문구, 보존 원본을 함께 제시했다.

## 고정 체크리스트·검증

- **주인공 1 / 분류**: 추천 시나리오 하나와 보조 행 하나. 375 첫 화면에 두 제목·시작 버튼이 보인다. 가공 시나리오 축소 방향 유지.
- **면/노선색**: 채움 면은 버튼, hero는 좌측 3px 선, 아래 행은 hairline. hero의 기존 색과 카드/분류 체계는 임의로 바꾸지 않았다.
- **영어 간판·카피**: 시나리오 제목·설명은 PM의 TKT-114 r2 카피와 일치. 사용자 본문의 운영 용어는 B1. 공통 목업 식별 배너는 유지해야 한다.
- **375 overflow / 타깃**: 375/390/1440 × 다크/라이트 **6조합 가로 overflow 0**, 타깃 I1. 제목·본문 잘림 없이 직접 캡처를 확인했다.
- **배지·10초**: 이 독립 목업에는 공통 S 역 배지/상단바가 없으므로 모선 정체성 정합의 근거로 쓰지 않는다. 스플래시 10초는 범위 밖.
- **라이트**: OS 라이트에서도 배경 `#0A0F16`·글자 `#F3F6FB`가 그대로인 **고정 다크 목업**. 라이트 설정에서 판독은 가능하지만 실화면의 라이트 토큰 검수를 대신하지 않는다. [라이트 설정 375](UX-TKT-102-r15-assets/simhub-375-light.png), [1440](UX-TKT-102-r15-assets/simhub-1440-light.png).
- **조작 범위**: Tab으로 두 버튼을 순서대로 방문했고 Enter/클릭 뒤 URL은 그대로다. href 0·script 0인 정적 시안이므로 실제 시뮬 시작/딥링크 성공을 주장하지 않는다. 기존에도 이동 동작은 없었으며 별도 앱 장애로 세지 않았다.
- **빌드/정적 규칙**: 격리 Pages-base build 성공, Vite 6.4.3 / Node 24.19.0 / 48 modules. Chromium localhost:4196, 브라우저 오류·경고 0, 외부 의존·script 0. [재현 스크립트](UX-TKT-102-r15-assets/check.cjs).

PM에 B1의 본문/운영 메모 분리, I1·S1의 목업 반영 범위를 요청한다. 이번 변경은 리뷰·증거·history·수신함뿐이며 앱·목업·스펙·티켓/보드 상태는 무수정, commit/push 없음. Safari/WebKit·실제 배포·실제 시뮬 화면은 미검증이다.
