문서 상태: 작성완료

# 의사결정 기록 (D-###)

이 저장소는 통합 모선이다. **편입되는 저장소들의 구조 결정도 여기에 남긴다.** 맥락 없이 읽는 에이전트가 이 문서만으로 경위를 복원할 수 있어야 한다.

상태: `제안됨` / `확정` / `폐기`. 확정 권한은 PO. 전사 공통 규칙은 `../CLAUDE.md`, PO 질문 창구는 `../ASK.md`.

---

## D-001 PM 주체를 Claude로 이관 · 확정 (2026-08-16)

- **배경**: 기존 AGENTS.md는 Codex를 "문서 중심 프로젝트 매니저이자 오케스트레이터"(+디자이너 역할)로 규정했다. 저장소마다 PM이 달라 PO 질문 창구가 분산됐다.
- **PO 발언**: "매번 돌아가면서 내가 물어보니 정신이 없었어", "너가 pm으로 변경하고 codex 시켜"
- **결정**: **Claude = PM(기획·설계·QA 리드·티켓 발행·리뷰·커밋), Codex = 구현 전담.** 아키텍처 가드레일·문서 규칙·design/ 트랙 산출물은 전부 보존한다.
- **후속**: PO가 "codex agent들도 재정산할거야, 필요시 합쳐도 돼"(2026-08-16)라고 밝혔다 — 기존 오케스트레이터/디자이너/워커 레인 구성은 재정산 대상이며, 재정산 결과가 나오면 AGENTS.md를 다시 개정한다.

## D-002 트렁크 = `codex/v0.6.0-line` · 확정 (2026-08-16, PO "그렇게 해")

- **배경**: `main`은 2026-06-18에 정지(v0.2.0)했고 실제 개발은 `codex/v0.6.0-line`(VERSION 0.6.0, +36커밋)에 있었다. 이를 모르고 main 기준으로 작업한 UX 개편(TKT-007)·인프라 파일은 `claude/ux-overhaul-stale-base-2026-08-16` 브랜치에 보존돼 있다 — **병합 금지, 참고용.**
- **결정**: 앞으로 모든 작업의 기준은 `codex/v0.6.0-line`. main 승격(병합)은 정리 후 별도 결정.
- **부수 효과**: 문서 언어 질문(Q-002)은 자동 해소 — 트렁크 문서는 이미 한국어다.

## D-003 통합 모선 지정 + 편입 3종 · 확정 (2026-08-16)

- **PO 발언**: "목표는 일단 workaround.co.kr에 가능한 내용을 통합하는건데", "화이트채플 관련 브랜치 개발건도 내 웹사이트에 들어오게 할거야"
- **결정**: 이 저장소를 모선으로 ①developer advisor ②화이트채플(bitter_sweet_testbed) ③追加 1종(레포 확인 중, `../ASK.md` Q-013)을 편입한다.

## D-004 확장 단위 = 모노레포 폴더, repo 신설은 예외 · 확정 (2026-08-16)

- **PO 질문**: "앞으로 홈페이지 확장할 때 해당 기능이 다 repository를 파야 하냐"
- **결정**: **아니다. 새 기능의 기본 배치는 이 모노레포 안의 폴더다.** 화면 = `frontend/src/modules/<기능>/`, 백엔드 = `services/<기능>/`, 정적 게임류 = `services/arcade/<게임>/`(TKT-028 게임 공간). 독립성은 repo 경계가 아니라 **계약**(자체 README·Dockerfile·`/health`·환경변수 문서)으로 확보한다 — `services/`에 파이썬·스프링 서비스가 이미 이 방식으로 공존한다.
- **repo 신설 예외 3조건**: ①플랫폼 밖 독자 배포/공개 ②이질 툴체인·대용량 바이너리 ③실험 단계(졸업 시 subtree 흡수).
- **편입 절차**: `git subtree add --prefix`로 원 레포 커밋 이력을 보존하며 흡수 → 원 레포는 아카이브(화석 main 방지).
- **가드**: 티켓에 `scope`(수정 허용 파일 화이트리스트) 필드를 도입해 에이전트가 자기 폴더 밖을 건드리는 사고를 규칙으로 막는다 (화이트채플 works 체계에서 이식). 서브모듈은 채택하지 않는다 — 이 조직의 브랜치 화석화·워킹트리 공유 사고 이력에 실패 모드를 추가하기 때문.

## D-005 정적 자산 서비스 계약 신설 · 확정·시행 (2026-08-16)

- **배경**: TKT-028(Flash 게임 공간)의 선행 조건이 "service-policy.md의 정적 자산 계약"인데 실제 문서에 그 계약이 없었다. 화이트채플(순수 정적 게임)도 `/health`를 줄 프로세스가 없어 필수 서비스 계약을 만족할 수 없었다.
- **결정**: 정적 자산 서비스는 다음 4개로 계약을 만족한 것으로 본다 — ①빌드 산출물 경로와 빌드 명령 ②reverse proxy(Caddy) 라우트 prefix ③정적 서버 컨테이너가 대신 응답하는 `/health` ④자산 출처·권리 표기. `docs/service-policy.md`에 절 추가 완료.
- **경위**: PO가 편입 시작을 승인("그전에 시작해도 돼", 2026-08-16)함에 따라 시행. 세부 형태에 PO 이의가 있으면 조정한다.

## D-008 advisor subtree 편입 시행 · 확정 (2026-08-16, PO "어 그렇게 해 흡수하고 디자인이 다른 건 백로그로")

- **무엇을**: `~/Project/developerAdvisor`(main, dev-001~017 완료 상태)를 `services/advisor/` 로 subtree 흡수 — 이력 보존(dev 커밋 23건 포함). 새 위치에서 `service/run.sh test` 통과 확인. collab 2레인·콘텐츠 불가침 규칙은 폴더 안에서 유지, `agent/codex/*` 브랜치·PR 규칙은 폐기(모선 규칙 적용). 편입 고지를 폴더 CLAUDE.md/AGENTS.md 에 삽입.
- **디자인 분리 원칙 (PO)**: 기능 편입과 디자인 통합을 분리한다 — 디자인 차이는 **백로그 티켓**으로: TKT-079(디자인·프레임 통합), TKT-080(서비스 계약 마감 — `/health` 부재 확인됨).
- **후속**: 원 레포(`~/Project/developerAdvisor`)는 아카이브 대상, 신규 커밋 금지. 10분 주기 Codex 루프는 새 경로 기준으로 재구동(에이전트 재정산 대상). `newProject/developer-advisor` 낡은 사본은 삭제 후보(PO 확인).

## D-007 화이트채플 subtree 편입 시행 · 확정 (2026-08-16)

- **무엇을**: `bitter_sweet_testbed`(공용 브랜치, WC 커밋 40건)를 `services/arcade/whitechapel/` 로 subtree 흡수. 게임 무관 학습 폴더(language·rust·study_book)는 폴더에서 제거(이력 보존). 새 위치에서 `npm test` 37/37·`npm run build` 통과 확인. 흡수분에 미리뷰 작업 WC-212·213 포함 — 화이트채플 보드 기준 교차 리뷰 대상.
- **후속**: 라우팅·매니페스트 연결은 TKT-078(v0.7.0). **원 레포는 아카이브 대상** — GitHub 아카이브 처리는 PO 소관, 그 전까지 원 레포에 신규 커밋 금지.

## D-006 화면 연계 = 같은 오리진 경로 라우팅 + 서비스 매니페스트 · 확정 (PO 2026-08-18 J4)

- **제안**: 편입 화면은 서브도메인이 아니라 **경로**로 연다 (`/advisor`, `/arcade/whitechapel`) — 같은 오리진이라 세션·로그인 공유가 무비용. 셸 내비게이션은 각 화면이 등록한 매니페스트(`{id, 이름, 노선색, 진입 경로, health, 렌더 방식(module|static|link)}`)를 렌더해 화면이 늘어도 셸 코드는 불변. iframe은 최후 수단.
- **상태**: 확정.

## D-009 공개 사이트의 LLM 기능은 반드시 gateway 경유 · 확정 (PO 2026-08-18 J4)

- **배경**: PO 질문 — "블로그에 Ollama 쿼리를 두면 docker 로 가둬도 가능할까? 호스팅 서버와 Ollama 서버가 다른데 중간에 gateway 까지 있어야 하나?"
- **제안**: 가능하되, **공개 트래픽이 RTX5070 Ollama 에 직접 닿는 경로는 만들지 않는다.** 경로는 항상 `Caddy(TLS) → gateway → 큐/정책 → RTX5070 Ollama`. docker 격리는 서버 탈취 반경을 줄일 뿐이고, 실제 위험(GPU 자원 고갈·남용·프롬프트 인젝션·게이밍 중 다운)은 **호출 경로 통제**로 막는다:
  1. gateway 에서 rate limit + 요청 큐(동시 1~2) + 입력 길이 상한
  2. 프롬프트는 서버측 고정 템플릿(사용자 입력은 변수로만) — 시스템 프롬프트 노출·탈취 방지
  3. RTX5070 인바운드는 호스팅 서버 IP 만 허용, Ollama 는 공인망에 절대 미노출
  4. Ollama 다운 시 degraded 응답(기존 ollama-policy 의 graceful degradation 그대로)
- **근거**: 기존 아키텍처 원칙("서비스는 Ollama 직접 의존 금지, worker/gateway 정책 경로")의 공개망 확장일 뿐 — 새 구조가 아니다. gateway 는 이미 이 목적으로 존재한다.
- **상태**: 확정. 블로그 질의 위젯 티켓은 TKT-068(RTX5070 Ollama 구동)·TKT-004(오프로드 계약) 뒤에 발행.

## D-010 여행 노선 (Line V · Voyage) 신설 · 확정 (2026-08-17, PO 지시)

- **PO**: "다음달에 동유럽 가는데 — 준비 / 과정(아침마다 오늘의 여행 지침서) / 여행 후 기록을 내 웹사이트에. 단순 기록이 아니라 콘텐츠였으면. 플랫폼이었으면. 길잡이가 될 수도, 게임 보드판 같은 게 될 수도."
- **결정**: 별도 repo 가 아니라 **모선의 새 노선**으로 (D-004). 노선 문자 `V`(Voyage — 구 `--line-v` 문자 재사용, 색은 신규 배정). 여행 = 노선, 도시 = 역, 여정 = 노선도. 지하철 메타포가 그대로 여행 메타포가 된다.
- **3구간 구조**:
  1. **개찰구 앞 (준비)** — 체크리스트·경로 계획·준비물. 출발 전 공개.
  2. **운행 중 (여행)** — 매일 아침 **"오늘의 운행 안내"**: 오늘 도시/이동편/볼 것/날씨/한 줄 미션. 모바일 우선(현지에서 폰으로), 스플래시 플랩 보드 문법 재사용 후보. LLM 초안(D-009 경로) + 본인 확정 가능.
  3. **종착 후 (기록)** — 역(도시)별 아카이브: 사진·글, 블로그 본선과 상호 링크.
- **게임 보드 요소**: 역 도장(방문 스탬프), 노선도 위 현재 위치 pulse 가 실제 여행 위치, 일일 미션 달성 표시.
- **일정 제약**: 출발이 다음달 — 준비 구간 화면부터 역순 우선. 스펙 = claude(TKT-084), 구현 = `[FE]`.

## D-011 공개 호스팅 = Cloudflare Pages + Tunnel 하이브리드 · 확정 (PO 2026-08-18 "채택")

- **결정**: 정적 프론트(Vue 빌드 산출물)는 **Cloudflare Pages**, 동적 API 는 **Cloudflare Tunnel → 홈서버 Spring gateway**. 브라우저는 기존대로 같은 오리진 `/api/*` — Pages Function 이 Tunnel 호스트로 프록시. RTX5070/Ollama 는 계속 gateway 뒤 내부망 (D-009 불변).
- **효과**: 홈서버·회선이 죽어도 블로그·여행 콘텐츠는 생존, 동적 기능만 degraded. 집 IP 인바운드 개방(80/443 포워딩) 불필요 — Tunnel 은 아웃바운드만.
- **근거**: `../reports/2026-08-18-hosting-cf-options.md` (부관 조사).
- **후속**: TKT-085 방향 갱신(Caddy 는 내부 개발용으로 강등), Pages 프로젝트·Tunnel 인증은 PO 계정 작업 병행.

## D-012 시뮬 2종은 "재미 공간"으로 묶어 단일 진입 · 확정 (PO 2026-09-09)

- PO: "elevator simulator 이랑 taxi district lab 는 그냥 약간 재미있는 공간 이런 느낌으로 해서 묶어서 넣고 — 노출을 많이 하지 말고 하나의 entrypoint 로 두라는 거야. 제목명은 기믹으로. 영화나 노래 이름이나 뭐 은유법 같은 거."
- 결정: 노선도의 E·T 지선을 제거하고 기존 Sim Hub(page `simhub`)를 유일한 진입점으로 승격. 명칭은 PM 선정 **"미스터리 트레인 (Mystery Train)"** — 짐 자무시 영화이자 엘비스 노래, 그리고 "노선도에 없는 열차"라는 지하철 은유의 삼중 레퍼런스. sub 라벨 `심야 임시 운행`.
- 대안(PO 교체 가능, lines.js 한 곳 수정): "나이트 온 어스"(자무시 택시 영화) / "심야차고지". 명칭 교체는 코드 구조에 영향 없음.
- 시행: TKT-101. `/elevator` `/taxi` 딥링크는 유지 — 제거하는 것은 정면 노출이지 기능이 아니다.

## D-013 노선 신설 vs 정류장 추가 기준 · **확정** (PO 2026-09-10 "1. 승인")

- 발단: PO "voyage line 을 만들고 첫 번째 정류장이 east europe 인 게 낫지 않아?" + "그렇게 개발되고 있던 모든 페이지 요소 검토."
- 제안: 노선=상설 공간(상한 ~9), 정류장=인스턴스. 리트머스 "두 번째 사례가 오면 이름이 거짓말이 되는가". 전수 감사: `design/ia-audit-2026-09-09.md` (전환 대상: Line V 구조, 재미 공간 통합 / 예방: P·D 노선).
- 확정: PO 2026-09-10. 후속 — V 컬렉션 전환은 PM 스펙 먼저, S 확장(028/078 합류)은 티켓 조정 후, P·D 는 스펙 시점 반영, Advisor 시즌 수명주기는 별도 검토.
- **부관 교차 감사 합산 (2026-09-10)** — 방향 전 항목 일치. 반영한 정밀화: ①V — lines.js 노선명은 이미 일반형("여행 노선"), 실제 전환 대상은 화면 헤더·전역 import·주소·방문 id·저장 키·백업이며 V01/V02/V03(화면 코드)과 여행 번호의 표기 충돌 정리 필요 ②S — 028/078 합류는 감사만으로 티켓 범위를 확장하지 않고 PM 이 티켓/스펙을 조정한 뒤 수행 ③P — 즉시 분리 대신 '취향 체험 유형→결과 후보' 계층, 독립 설문 생길 때 정류장 추가 ④**신규 발견: Advisor 시즌이 단일 seasonStart 고정 — 28일 경과 후 스탯 적립 거절(seasonStats.js:28,39-42). 반복 시즌 수명주기 설계 공백, 별도 검토 티켓 필요.**

## D-014 공개 Pages 운영 원칙 (감사 AS-R006 후속) · **확정** (PO 2026-09-11 — docs 공개 승인, 나머지 PM 조치 추인)

- 발단: 감사팀 AS-R006 — 공개 번들의 외부 공유 대화 링크(개인 맥락 노출), SPA 딥링크 HTTP 404 vs TKT-096 완료조건, 트렁크 무보호 자동 배포, 공개 저장소의 운영 docs 159개.
- PM 조치·제안:
  1. **외부 링크 기본 금지** — 공개 번들에서 claude.ai 공유 대화 URL 즉시 제거(voyage.js, 2026-09-11). 이후 외부 URL 은 티켓에 목적·소유자 기록 시에만 허용. PO 가 원문 공개를 원하면 되돌린다.
  2. **딥링크 HTTP 404 는 정적 호스팅의 구조적 한계로 수용** — 화면 렌더 복구가 완료 기준. TKT-096 완료조건을 이에 맞게 정정.
  3. **트렁크 무보호는 단독 운영 기간 수용 리스크** — 협업자 추가 또는 v0.7.0 태그 시 branch protection 재검토.
  4. **docs 공개 유지 확정** — PO "docs는 공개해도 돼" (Q-014 (a)). AGENTS.md 의 비공개 문구 폐기, 문서에도 코드와 같은 민감정보 기준 적용. (GitHub Free 는 공개 저장소에서만 Pages 무료 — 비공개 전환은 호스팅 재설계를 뜻함.)
- AS-R007 채택: 묶음 커밋 시 커밋 본문에 티켓별 파일 목록을 기재한다 (patch 경계 보존).

## D-015 노선도 재구성 — 분류가 노선, 서비스가 정류장 · 제안됨 (PO 2026-09-14 "차라리 분류별로 노선도를 빼는 게 맞지 않아?")

- 현행: 노선 8개(B·A·W·R·V·D·S·P)가 각각 지선, 정류장 1~3개 — 은유가 헐겁다.
- 제안: **노선 = 3분류**(경험 / 학습·놀이 / 운영), **정류장 = 서비스**(경험선: 블로그 B → 여행 V / 학습·놀이선: Advisor A → 미스터리 트레인 S → 발견 D → 취향 P / 운영선: Work W → Runtime R). 미개통 D·P 는 "연장 예정 구간" 점선. 환승 홀은 세 노선이 만나는 환승역. 서비스 문자 배지는 역 번호처럼 작게 유지(컨셉 단서).
- 함의: lines.js 를 노선 3 + 정류장 8 구조로 전환(D-013 과 정합 — 노선=상설 공간). 노선색 3개로 축소, 서비스별 색은 각 화면 내부 액센트로만. 홈 아래 목록은 노선별 정류장 열거로 단순화(별도 3묶음 헤더 불필요 — 노선이 곧 분류).
- **대안 B (PO 추가 2026-09-14 "순환선을 하나 만들고 노선을 거기에서 빼도 돼 — 사이트를 순환선으로, 그걸 애초에 컨셉으로")**: 사이트 = **순환선 1개**. 원 위의 역 = 서비스 8개, 원의 호 = 분류 3구간(경험·학습·놀이·운영, 구간별 색), 미개통 = 점선 구간, 환승 홀 = 순환선 기점. PM 권고 = **B** — 상징성(2호선), 모바일 비율(원은 안 눌림), 스플래시 원형 배지와 일관, Line V "순환선" 명명과 호응. A(3노선 교차)는 비교안.
- r6 결과(2026-09-14): B(순환선만)·A(3노선 교차) 모두 PO 의 상(像)이 아님 — PO "순환선에서 환승으로 뻗어나가는 생각". → **안 C = 순환선 + 환승 지선** (PM 종합, 권고): 순환선 = 사이트(단색 굵은 링, 구간명은 호를 따라 글자), **환승역 = 노선(서비스 8)**, 각 환승역에서 **노선색 지선이 바깥으로** 뻗고 지선의 정류장 = 하위 화면(B: 아카이브→글 상세→스튜디오 / V: 준비→오늘→기록 / S: 격납고→엘베→택시→화이트채플 / A·W·R: 1역). 미개통 D·P 는 점선 stub. 12시 환승 홀 기점. 세 층(분류→노선→화면)이 한 그림. r8 목업 후 PO 확정.

## D-016 톤 전환은 브랜치 `codex/v0.7.0-tone` 에서 · 확정 (PO 2026-09-14 "현재 홈페이지 다 목업으로 대체해. 그럴려면 새로운 브랜치를 따는 게 맞지 않니?")

- 이유: 트렁크는 push 즉시 Pages 라이브(D-014-3) — 여행 중 실사용 화면을 전면 교체 작업 중 상태로 둘 수 없다.
- 운영: 전 에이전트 작업 브랜치 = `codex/v0.7.0-tone`(같은 워킹트리, 체크아웃 전환). 트렁크는 **동결** — 예외는 여행 콘텐츠(voyage 데이터)·핫픽스를 PM 이 cherry-pick. 목업(`frontend/public/mockups`)은 양쪽 공통.
- 미리보기: TKT-115 로 트렁크 배포에 `/next/` 동봉. 병합 = v0.7.0 릴리스 = 감사팀 릴리스 게이트(RELEASE-v0.7.0.md) 통과 후.
- 구현 시리즈: TKT-110(기반+스플래시) → 111(홈 순환선) → 112(여행 3종) → 109(여정 노선도) → 113(블로그) → 114(스튜디오·격납고·운영). 105·108 은 브랜치에서 이어서.

## D-017 advisor 서버측 LLM 직접 호출 예외 · 확정 (PM 2026-09-14, TKT-080 질문 답변)

- 원칙 D-009(공개 LLM 기능은 gateway 경유)는 **브라우저→공개망** 경로에 적용. advisor `service/` 가 서버측에서 Anthropic 을 직접 호출하는 구조(D-008 편입 당시)는 예외로 허용: ①기본 프로필 `mock`(외부 호출 0) ②`claude` 프로필은 환경 변수로만 키 주입 ③호출 실패 시 mock provider 로 격리 ④브라우저 공개 경로는 `/api/services/advisor/**` gateway 프록시. 근거·환경 변수 표는 `services/advisor/README.md`.

## D-018 순환선 심볼 = C안 "열린 기점" · 확정 (PO 2026-09-14 "순환선 심볼은 C안으로 가자")

- 단색 링에 12시 기점 양옆을 비운 형태(문이 열리는 순간 암시). 상단바 24px·스플래시 대형·파비콘 16px 공통. 시안: `frontend/public/mockups/loop-symbol.html`(codex-8 r9). 구현: TKT-126(`SiteLoopSymbol.vue` 교체 + 파비콘).
- PM 메모: 16px 에서 끊긴 링으로 읽힐 위험은 기점 점을 남기고 틈을 좁게(≈36°) 두어 완화 — 126 에서 파비콘 실측.

## D-015 보완 — 안 D 확정 후보 + 노선명 (PM 2026-09-14, PO 피드백 반영)

- PO: "노선은 내용은 1안(r11 — 하위 화면까지 역으로), 형태는 2안(r12b — 십자 3노선). 노선명은 '경험선/학습·놀이선/운영선' 말고 이전 네이밍 규칙처럼 은은하게."
- 안 D = **순환선 + 환승역 3 + 노선 3(십자)**, 노선 위 큰 역 = 서비스, 작은 역 = 화면. 노선명 PM 제안(PO 확정 대기): **기록선 Archive Line**(블로그·여행) · **실험선 Lab Line**(Advisor·미스터리 트레인·발견·취향 — 제품 방향 "실험 놀이터") · **기지선 Depot Line**(Work·Runtime — 차량기지=보호 구역). 목업 r12c.

## D-015 안 E — 환승 홀 교차형(링 없음) · 후보 (PO 2026-09-14 "노선도 모양은 저 원 말고 그냥 환승역으로")

- 순환선 링을 그리지 않는다. **환승 홀 = 세 노선이 만나는 환승역**(중앙), 기지선 서·기록선 동·실험선 남으로 갈라짐(T자). 큰 역 = 서비스, 작은 역 = 화면. 목업 r13. "사이트 = 한 노선" 컨셉은 심볼(C안)과 노선명으로만 유지.
- D-013 정합: 노선 3 = 상설 공간, 서비스·화면 = 정류장.
