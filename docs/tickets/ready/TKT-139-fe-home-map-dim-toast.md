문서 상태: 작성완료

# TKT-139 `[FE]` 홈 노선도 — 기지선·예정 역 흐림 + 누르면 같은 화면 토스트("준비 중")

- 상태: ready · P1 · 담당: codex-1 · 의존: TKT-136 finished. 브랜치 `codex/v0.7.0-tone`. **병합 전 반영**(첫인상). **UX 1순위·[반박]/[구체화 질문] 의무.**
- 근거: PO 2026-09-14 — 기지선(Work·Runtime)은 "약간 흐린 형태, 그래도 있는 티는 내게. 누르면 under construction 형태로, 화면이 넘어가진 않게, 같은 화면에서 간단하게(팝업 아님)". PM 해석: **토스트**(누른 자리 근처에 한 줄 안내가 잠깐 떴다 사라짐). 예정 역(발견·취향)도 같은 처리(PM 가정 — 이의 시 조정). D-021.
- scope: `frontend/src/data/lines.js`(역 `access: 'protected'|'planned'` 플래그), `frontend/src/components/JunctionMap.vue`(흐림 스타일·토스트), `frontend/src/styles.css`, `JunctionMap.e2e.mjs`

## 목표
1. **흐림**: 기지선 두 역(W·R)과 노선·이름을 약 55% 불투명도(다크/라이트 모두 대비 3:1 이상은 유지)로. 예정 역(D·P)은 현행 점선 + 같은 흐림. "있는 티"는 남는다 — 지우지 않는다.
2. **토스트**: 흐린 역(SVG 역·목록 행 모두)을 누르면 **화면 이동 없이** 누른 요소 근처(모바일은 하단 고정 한 줄)에 `보호 구역 · 준비 중` / `예정 노선 · 준비 중` 한 줄이 2.5초 표시 후 사라짐. 팝업·모달·페이지 전환 금지. `role="status" aria-live="polite"`. 키보드 Enter/Space 도 같은 동작. 연속 클릭 시 토스트 1개만.
3. 기존 딥링크(`/work` `/runtime`)는 유지 — 주소 직접 입력은 그대로 열린다. 노선도·목록에서의 진입만 토스트로 막는다.
4. 카피: `보호 구역 · 준비 중`(기지선), `예정 노선 · 준비 중`(D·P). 영어 없음.

## 완료 조건
1. [ ] 375/1440 × 다크/라이트에서 W·R·D·P 흐림 + 나머지 역 대비 유지, 라벨 대비 3:1 이상 실측.
2. [ ] 클릭·Enter 시 URL 불변, 토스트 1개, 2.5초 뒤 제거 — E2E.
3. [ ] 기존 JunctionMap E2E·136 검사(겹침 0) 그린, build.

## 질문/에스컬레이션
- 없음.

## 리뷰 기록
- 없음.
