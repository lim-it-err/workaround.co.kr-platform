문서 상태: 작성완료

# TKT-161 `[UX]` 접근성 검수 — VoiceOver·키보드만으로 전 경로

- 상태: ready · P2 · 담당: codex-8(디자이너) · 의존: TKT-145 finished(병합 후 라이브에서 해도 됨). 브랜치 `codex/v0.7.0-tone`. 앱·목업·티켓·보드 무수정.
- 근거: REV-TKT-138~144·AS-R009 전부 "Safari/WebKit·VoiceOver 미검증". 톤 원칙의 접근 이름 기준·홈 토스트 `role=status`·정차역 시트 포커스 트랩이 실제 보조기기에서 읽히는지 본 적이 없다.
- 대상: 메인 정적 8경로 + 글쓰기 시트 + 정차역 시트 + 홈 흐림 역 토스트 + Advisor 3표면 + 미션 제출→리뷰 흐름. macOS VoiceOver(Safari·Chrome) + 키보드만.
- 산출: `docs/reviews/UX-A11Y-2026-09-15.md` — 화면별 [블로커]/[중요]/[제안], 우선순위 5, 후속 티켓 후보(파일:라인). 수신함 한 줄.

## 완료 조건
1. [ ] 각 경로에서 Tab 순서·포커스 가시성·landmark·접근 이름 표(누락은 요소·파일:라인).
2. [ ] 토스트·시트·다이얼로그의 읽기 순서와 닫힘 복귀 포커스 기록.
3. [ ] 병합 전 필수 항목이 있으면 맨 위에 별도 표기.

## 질문/에스컬레이션
- 없음.

## 리뷰 기록
- 없음.
