# TKT-078

## 메타데이터

- 제목: 화이트채플 아케이드 편입 마감 — 라우팅·매니페스트·게임 공간 연결
- 우선순위: P1
- 대상 버전: `v0.7.0`
- 상태: `ready` (2026-09-15, PM 승격 — Pages 우선 범위)
- 문서 상태: `작성완료`
- 진행 판정: `v0.7.0 진행 시 가능` (TKT-028 게임 공간과 같은 버전 — 함께 진행 권장)
- 담당: codex
- scope: `infra/**`, `frontend/src/**`(내비/매니페스트 표시부만), `services/arcade/whitechapel/README.md`
- 의존: TKT-028 (게임 공간·플레이어 틀), D-005·D-006 (decisions.md)

## 목표

subtree 로 흡수된 `services/arcade/whitechapel/`(2026-08-16, 코드·테스트 편입 완료)을 실제 사이트에서 접속 가능하게 만든다 — `/arcade/whitechapel` 경로 서빙 + 게임 공간 목록 노출.

## 작업 내용

- Caddy(또는 현행 reverse proxy 구성)에 `/arcade/whitechapel` → `services/arcade/whitechapel/whitechapel/standalone.html` 정적 서빙 라우트를 추가한다. 정적 서버 컨테이너가 `/health` 를 대신 응답한다 (D-005).
- 빌드 파이프라인: 배포 시 `npm run build` 로 standalone.html 을 생성하는 단계를 배포 번들에 포함한다.
- 서비스 매니페스트(D-006)에 화이트채플 항목 등록: `{ id: 'whitechapel', 이름: '화이트채플', 진입: '/arcade/whitechapel', 렌더: 'static' }`. TKT-028 게임 공간 목록이 이 매니페스트를 읽어 렌더한다.
- 게임 로직·게이트웨이 결합 금지 — 게이트웨이는 어떤 역할도 갖지 않는다 (정적 경로는 Caddy 직결).

## 완료 기준

- 로컬 compose(또는 배포 환경)에서 `/arcade/whitechapel` 접속 시 게임이 플레이 가능하다.
- 게임 공간(아케이드) 화면에 화이트채플이 목록으로 보이고 진입할 수 있다.
- `services/arcade/whitechapel` 안에서 `npm test` 37+ 전부 통과 유지.
- 관련 문서(README 라우트 표, 매니페스트 문서) 갱신.

## 범위

- 포함: 라우팅, 배포 번들 빌드 단계, 매니페스트 등록, 문서.
- 제외: 게임 자체 개선(WC-### 티켓 소관), WC-205(Sonnet 악몽 난이도), 원 레포 아카이브 처리(PO 소관).

## 질문/결정 기록

- 편입 경위: decisions.md D-003·D-004·D-005. 흡수 시 미리뷰 작업 WC-212·213 포함됨 — 화이트채플 보드(`services/arcade/whitechapel/docs/works/BOARD.md`) 기준으로 교차 리뷰 후 반영할 것.


## PM 메모 (2026-09-13)

- **배포 타깃은 GitHub Pages 우선**: TKT-103 패턴으로 `prepare-github-pages.mjs` 에 `--whitechapel-dist services/arcade/whitechapel/...`(standalone.html) 병합 → `/arcade/whitechapel/` 경로. Caddy 라우트는 자가 호스팅용 보조로 유지. FE 카드는 TKT-107.

## PM 추가 (2026-09-15, ready 승격)
- 상태 `ready` · P1 · **codex-2** · 큐: 159 뒤. 범위는 위 PM 메모(2026-09-13)대로 **GitHub Pages 우선** — `infra/public-site/prepare-github-pages.mjs` 에 whitechapel standalone 병합(`/arcade/whitechapel/`), TKT-028 게임 공간·Caddy 라우트는 이 티켓에서 제외(자가 호스팅 보조는 후속). 완료 기준 1·2 는 "Pages 병합 dist 에서 `/arcade/whitechapel/` 200 + 게임 플레이 가능" 으로 읽는다. need_review 진입 시 TKT-107(codex-1) 착수 가능(의존성 완화).
