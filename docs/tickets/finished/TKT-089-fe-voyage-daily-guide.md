# TKT-089
## 메타데이터
- 문서 상태: 작성완료
- 제목: Line V 일일 운행 안내 — "오늘의 여행 지침서" 화면
- 우선순위: P1 / 상태: `finished` / 담당: `[FE]` / 의존: TKT-088(모듈 골격)
- 진행 판정: `진행 가능`
- scope: `frontend/src/**` voyage 모듈 내
- 데이터: `voyage.js` days[] (수정 금지)
## 목표
날짜 기준 오늘(여행 중이면 해당 일, 아니면 선택한 일)의 안내판: 오전/오후/저녁 3단 + 운전 시간 배지 + tip. **모바일 우선**(현지에서 폰으로 보는 화면이 1급). 날짜 이동(전날/다음날), 전체 일정 축소판(11일 도트 스트립, 현재 위치 pulse).
## 완료 게이트
- build + 모바일 375px 실측 (이 화면은 모바일 스크린샷 필수)
- driveMin>0 이면 운전 배지·휴식 규칙 표시, 0이면 "운전 없는 날" 강조

## 작업자 산출물 (2026-08-18, codex-1)

- 브랜치: 트렁크 `codex/v0.6.0-line` 직접, 커밋 없음.
- `VoyageDailyView.vue` 신설 — `voyage.js`의 11일 일정으로 날짜 이동, 도트 스트립/pulse, 오전·오후·저녁, 운전 시간·운전 원칙, tip을 렌더한다.
- `VoyageView.vue`를 추가해 V01 준비 화면과 V02 일일 안내를 연결하고, 화면 전환 시 스크롤을 상단으로 복원했다.
- 검증: `npm run build` 통과(Vite 6.4.3, 24 modules), `git diff --check` 통과.
- 브라우저 검증: 양 테마 확인, 0분 일정의 `운전 없는 날`, 10분 운전일, 5시간 35분 예외일과 휴식 규칙 표시 확인. 375×812에서 V02·11개 날짜 버튼 렌더 및 `scrollWidth=clientWidth=375`, 콘솔 오류 없음. 모바일 스크린샷 확인 완료.
