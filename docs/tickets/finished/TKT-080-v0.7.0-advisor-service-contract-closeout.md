# TKT-080

## 메타데이터

- 제목: advisor 서비스 계약 마감 — /health·라우트·매니페스트·빌드 게이트
- 우선순위: P1
- 대상 버전: `v0.7.0`
- 상태: `finished` (REV-TKT-080-r1 통과, PM 2026-09-14)
- 문서 상태: `작성완료`
- 진행 판정: `v0.7.0 진행 시 가능`
- 담당: codex
- scope: `services/advisor/service/**`, `services/advisor/README.md`, `infra/**`(라우트), `services/advisor/frontend/package.json`(빌드 게이트 한정)
- 의존: D-008

## 배경

advisor 는 subtree 흡수됐지만(D-008) 모선 서비스 계약에 공백이 있다 — Dockerfile 은 있으나 **`/health` 엔드포인트가 없다** (actuator 미도입, 커스텀 매핑도 없음 — 2026-08-16 확인). 또한 이 머신에서 frontend 빌드 게이트가 node 버전 비호환으로 돌지 않는다.

## 작업 내용

- `service/` 에 `/health` 추가 (spring-boot-starter-actuator 도입 또는 경량 컨트롤러 — 선택 근거 기록). 게이트웨이 헬스 집계에 등록.
- 서비스 등록 정보 문서화 (`services/advisor/README.md` 에 계약 표): 서비스 ID `advisor`, compose hostname, `/health`, 라우트 prefix `/advisor`, 환경 변수(`ADVISOR_PROFILE`·`ANTHROPIC_API_KEY`·`ADVISOR_AUTH_TOKEN`·CORS).
- 모선 compose 에 advisor 서비스 추가 (자체 `docker-compose.yml` 은 단독 개발용으로 유지, 모선 통합 기동은 모선 compose 가 담당).
- CORS 기본값(`localhost:5173`)을 같은 오리진 경로 체계(D-006)에 맞게 재검토.
- frontend 빌드 게이트 복구: node ≥20.12 상향 또는 vite 버전 고정 — **환경 요구를 README 에 명시** (AGENTS.md 검증 게이트가 어느 머신에서든 돌아야 한다).
- LLM 접근 규칙 검토: advisor 는 Anthropic API 직접 호출 구조다. 모선 정책(서비스의 Ollama 직접 의존 금지)은 외부 유료 API 에 그대로 적용되지 않으나, 키 관리·장애 격리 관점의 예외 기록을 decisions 에 남긴다.

## 완료 기준

- `GET /health` 200, 게이트웨이 집계에 advisor 상태 표시.
- 모선 compose 로 advisor 가 함께 기동된다.
- README 계약 표 완비, 빌드 게이트가 문서된 환경에서 재현 가능.
- `service/run.sh test` 그린 유지.

## 구현 결과 (2026-09-13, codex-2)

- actuator 의존성을 추가하지 않고 `GET /health` 전용 경량 컨트롤러를 추가했다. 인증 인터셉터 범위(`/api/advisor/**`) 밖에서 `200 {"status":"ok","service":"advisor"}`를 반환한다.
- gateway 서비스 레지스트리에 `advisor`와 `ADVISOR_SERVICE_URL`을 추가했다. 집계 health와 `/api/services/advisor/**` 프록시가 같은 서비스 계약을 사용한다.
- 모선 `infra/docker-compose.yml`에 `advisor` 서비스, 영속 볼륨, mock 기본 프로필, 키·인증·CORS 환경 변수 및 gateway 의존/내부 URL을 추가했다. 단독 개발용 `services/advisor/docker-compose.yml`은 유지했다.
- 운영 화면은 `/advisor/` 같은 오리진 경로를 사용하므로 별도 CORS 허용이 필요 없음을 README에 기록했다. `ADVISOR_CORS_ALLOWED_ORIGINS`는 `localhost:5173` 단독 개발용 기본값과 쉼표 구분 목록을 지원한다.
- frontend의 지원 기준을 Node.js `>=20.12.0`으로 선언하고 README와 일치시켰다.
- Anthropic 직접 호출은 D-008 편입 당시 구조이며, 브라우저 공개 경로는 D-009대로 gateway를 통한다. 기본 Compose는 외부 호출 없는 `mock`, `claude` 프로필은 환경 변수로만 키를 받고 실패 시 mock provider로 격리한다는 근거를 README에 남겼다.

## 검증 결과

- `services/advisor/service ./run.sh test`: **35/35 통과**, failures/errors/skipped 0.
- `gateway`에서 JDK 21 명시 `mvn -q clean package`: **8/8 통과**, failures/errors/skipped 0.
- `services/advisor/frontend npm run build`: **99 modules**, production build 통과.
- `infra docker compose config --quiet`: 통과.
- `docker compose build advisor gateway`: 두 이미지 빌드 통과(Advisor 이미지 빌드 안의 전체 테스트 포함).
- Compose 네트워크에서 Advisor와 Gateway 실제 기동 후 `GET /api/services/advisor/health`: **HTTP 200**, `status=ok`; `GET /api/health`의 `advisor.status`: **ok**. 기존 호스트 8080 프로세스를 보존하기 위해 검증용 Gateway만 18080에 임시 바인딩했으며, 생성한 컨테이너와 네트워크는 검증 후 제거했다.

## 질문 / 남은 위험

- `docs/decisions.md`는 PM 판정·스펙 영역이라 구현자가 수정하지 않았다. D-008·D-009 및 README의 예외 근거로 충분한지, 별도 결정 ID가 필요하면 PM 최종 리뷰에서 기록해 달라.
- 실 Anthropic 키를 쓰는 `claude` full cycle은 비밀값 없이 수행하지 않았다. 이는 TKT-087의 PO 키 주입 검증 항목이며, 본 티켓의 mock 기본 기동·계약 완료 기준과 분리한다.
