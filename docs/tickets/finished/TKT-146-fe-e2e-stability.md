문서 상태: 작성완료

# TKT-146 `[FE]` E2E 안정화 — 스플래시 타이밍 허용치·병렬 실행 내성

- 상태: finished (2026-09-15, PM 통과) · P2 · 담당: codex-1 · 의존: 없음. 브랜치 `codex/v0.7.0-tone`. **UX 1순위·[반박] 의무.** 제품 코드 무변경(테스트만).
- 근거: REV-TKT-140-r1 [제안] — 배치 병렬 실행에서 `splashTone.e2e.mjs` 10초 타이밍 테스트 1건 실패, 단독 6/6. AS-R009 §후속(스위트별 base 혼합 계약).
- scope: `frontend/src/splashTone.e2e.mjs`, 그 외 `frontend/src/**/*.e2e.mjs` 중 고정 sleep·wall-clock 의존 부분.

## 목표
PM 게이트가 여러 스위트를 동시에 돌려도 타이밍 때문에 red 가 나지 않게 한다. 방법은 구현자 판단(허용치 ±, 조건 대기로 교체, 가짜 타이머) — 단 **제품 동작 기준(첫 방문 10초·재방문 3초)을 느슨하게 단언하지는 않는다**(정착 시각 검증은 유지하되 CPU 경합 여유만).

## 완료 조건
1. [x] 메인 E2E 전 스위트를 두 터미널에서 동시에 3회 실행 → 3/3 그린(실행 기록을 티켓에).
2. [x] `splashTone.e2e.mjs` 단독 6/6, 병렬 6/6.
3. [x] 고정 sleep 을 조건 대기로 바꾼 파일 목록과 이유 한 줄씩.
4. [x] `frontend/src` 제품 코드 diff 0.

## 구현 기록
- `frontend/src/splashTone.e2e.mjs` — 실제 CPU 경합에 기대던 고정 sleep 을 Playwright 가상 시계 진행과 DOM 소멸 조건 대기로 바꿨다. 첫 방문 9,999ms에는 스플래시가 남고 10,000ms에 전환되며, 재방문도 2,999ms/3,000ms 경계를 그대로 단언한다.
- 플랩의 난수 지연은 대표값으로 고정해 병렬 부하와 무관하게 같은 정착 시각을 재현한다. 첫 방문 최종 문구 정착도 9,300ms에 별도로 검증하므로 연출 완료 확인을 제거하지 않았다.
- `Date.now()` 기반 방문 시각 fixture도 같은 가상 기준 시각으로 통일했다.
- SIM의 30초 무조작 수용 조건 등 다른 E2E의 실제 시간 sleep 은 제품 시간 자체를 검증하는 별도 계약이고 이번 실패 원인이 아니어서 유지했다.
- 이 티켓의 변경 파일은 E2E 한 건뿐이며 `frontend/src` 제품 코드 변경은 0이다. 보드·티켓·당일 history는 수명주기 공유 파일이다.

## 검증 기록
- Pages 빌드: 메인 49 modules, Advisor 110 modules. root 빌드 49 modules. Pages 병합 산출물 참조는 메인 5 + Advisor 2로 확인했다.
- 스플래시 단독: 6/6 통과. 동일 스위트 두 프로세스 병렬: 각 6/6 통과.
- 메인 E2E 전 12스위트(58 tests)를 두 터미널에서 동시에 실행:
  - 1회차: 터미널 A 58/58, B 58/58.
  - 2회차: 터미널 A 58/58, B 58/58.
  - 3회차: 터미널 A 58/58, B 58/58.
- `git diff --check` 통과. 제품 화면 변경이 없는 테스트 전용 티켓이라 신규 시각 QA는 생략했고, 전체 E2E의 375/1440·다크/라이트 계약은 함께 통과했다.
- 남은 위험: Chromium 기준이다. Safari/WebKit·실 GitHub Pages·저사양 실기기 병렬 부하는 미검증이다.

## 질문/에스컬레이션
- 없음.

## 리뷰 기록
- 2026-09-15 codex-1: 구현 및 완료 게이트 충족, `need_review` 전환. commit/push 없음.
- 2026-09-15 PM: **r1 통과 → finished** — `docs/reviews/REV-TKT-146-r1.md`. 커밋은 147 r2 와 묶음(공유 파일).
