문서 상태: 작성완료

# TKT-141 `[FE]` 정차역 편집 시트 — 기본 3칸 + 나머지 접힘

- 상태: finished (2026-09-15, REV-TKT-141-r1 통과) · P2 · 담당: codex-1 · 의존: TKT-117 finished. 브랜치 `codex/v0.7.0-tone`. 병합 뒤 반영 가능. **UX 1순위·[반박] 의무.**
- 근거: PO 2026-09-14 [PM 의문] 4 승인.
- scope: `frontend/src/components/voyage/VoyageRouteMap.vue`(시트 폼), `VoyageRouteMap.e2e.mjs`

## 목표
시트 기본 노출은 **먹은 것 · 원화 금액 · 메모** 3칸. `식당명 · 현지 금액/통화 · 구글 지도 링크 · 사진` 은 `더 적기` details(hairline)로 접힘 — 값이 이미 있으면 자동 펼침. 저장 막대·포커스 트랩·Esc(119)·초안 계약(117) 무변경.

## 완료 조건
1. [x] 375 에서 시트 첫 화면에 3칸 + 저장 버튼이 스크롤 없이 보임.
2. [x] 값 있는 정차역은 접힌 칸 자동 펼침. 117 E2E 그린.

## 구현 내역

- 정차역 폼의 기본 노출을 `먹은 것 · 원화 금액 · 메모` 세 칸으로 재배치하고, 식당명·현지 금액/통화·구글 지도 링크·사진은 면 없는 `더 적기` hairline details 안으로 옮겼다.
- 저장 기록이나 시드에 추가 필드·사진 값이 하나라도 있으면 시트를 열 때 `더 적기`가 자동으로 펼쳐진다. 값이 없는 정차역은 접힌 상태로 시작하며 사용자가 직접 열고 다시 접을 수 있다.
- 저장 막대·입력 이름·사진 제한·지도 URL 검증·localStorage 구조·포커스 트랩·Escape와 호출자 포커스 복귀는 변경하지 않았다.

## 검증

- [x] 메인 unit `22/22`, 기본·Pages-base build 각 Vite `49 modules` 통과.
- [x] `VoyageRouteMap.e2e.mjs` Chromium `4/4` 통과 — 375·1440 × 다크·라이트, 무값 접힘·수동 왕복·기존값 자동 펼침·저장/새로고침 복원·사진·요금 갱신·포커스 트랩·Esc·API 요청 0·가로 overflow 0.
- [x] 375px 무값 시트에서 `scrollTop=0`인 채 세 기본 칸과 저장 버튼이 모두 뷰포트 안에 있음을 자동 측정했다. 테스트 날짜는 제품 일정과 무관하게 2026-09-14로 고정해 기존 `740km` 단언의 날짜 민감성도 제거했다.
- [x] `/tmp/tkt141-visual-20260915`의 375·1440 × 다크·라이트 접힘/펼침 캡처를 직접 확인했다. 기본 시트는 3칸·hairline·저장 행동의 위계가 분명하고 겹침이나 잘림이 없다. `git diff --check` 통과, commit/push 없음.

## 질문/에스컬레이션
- 없음. 승인된 정보 축약이 기존 UX·저장 계약과 충돌하지 않아 반박 사항 없이 구현했다.

## 남은 위험

- Safari/WebKit·실제 모바일 키보드·실 GitHub Pages 배포는 미검증이다.

## 리뷰 기록
- 없음.
- r1 (2026-09-15, PM): **통과 → finished**. `docs/reviews/REV-TKT-141-r1.md`.
