문서 상태: 작성완료

# TKT-154 `[FE]` Advisor 리뷰 화면 — 먼저 고칠 것 하나, 상태에 맞는 CTA

- 상태: finished (2026-09-15, PM 통과) · P2 · 담당: codex-1 · 의존: TKT-130 finished. 브랜치 `codex/v0.7.0-tone`. 레인: **Advisor(`services/advisor/frontend/`)** — codex-6 기동 시 codex-6, 미기동이면 codex-1 큐 뒤. 콘텐츠 불가침 3파일(`sampleContent.js`·`sampleProjects.js`·`courseVienna1900.js`) 수정 금지. **UX 1순위·[반박] 의무.**
- 근거: UX-ADVISOR-2026-09-14 Review [개선](`ReviewPage.vue:94,139,153,178,224`) — 점수·항목별 피드백·히든 케이스·평판·시나리오가 길게 쌓이고, 미제출 상태에서도 `재제출/다음 미션` 이 보인다. TKT-130 은 완료 요약(`기록에 저장됨`)만 다뤘다.
- scope: `services/advisor/frontend/src/modules/missions/pages/ReviewPage.vue`, 관련 store·E2E.

## 목표
결과 화면 상단 = 점수 한 줄 + **먼저 고칠 것 1개**(가장 큰 감점 항목) + `코드 고쳐서 재제출` 하나. 항목별 피드백·히든 케이스·평판·시나리오는 펼침(면 없음). 샘플 경고는 유지.

## 완료 조건
1. [x] 미제출 상태: 재제출/다음 미션 CTA 비노출, 대신 `제출하러 가기`(미션 편집으로).
2. [x] `다음 미션` 목적지가 실제 다음(코스 순서 또는 배우기 필터 문맥)일 때만 그 라벨, 목록으로 가면 라벨 `배우기로`.
3. [x] 375 결과 화면 첫 스크롤 안에 점수·고칠 것·CTA 모두 노출(스크린샷).
4. [x] E2E 3건(미제출·제출 후·다음 목적지), 46+ 그린.

## 구현 기록
- 제출 리뷰 상단을 `종합 점수 → 가장 큰 감점 항목 1개 → 코드 고쳐서 재제출` 순서로 재구성했다. 감점은 루브릭 배점과 획득 점수의 차이로 계산하며, 전체 설명은 항목별 펼침 안에 보존했다.
- 항목별 피드백·히든 케이스/퀘스트·평판·시나리오·설명 피드백을 배경 면 없는 hairline 펼침 행으로 접었다. 샘플/실시간 리뷰 경고와 TKT-130 완료 요약은 유지했다.
- 미제출은 `제출하러 가기`만, 리뷰 미수신 제출은 재제출만 노출한다. 출발 문맥이 실제 코스이고 다음 항목이 있을 때만 `다음 미션`과 정확한 코딩/게임/시뮬 목적지를 만들며, 그 외에는 `/learn`의 `배우기로`를 노출한다.
- `ReviewPage.vue`의 TKT-152 상세 콘텐츠 지연 로드용 import·hydrate 변경은 그대로 보존했다. 불가침 콘텐츠 3파일은 수정하지 않았다.

## 검증
- Advisor build: 114 modules, 경고 0.
- Advisor unit: 81/81.
- Advisor Chromium E2E: 52/52. 신규 3건은 미제출·제출 후 우선순위/375 첫 화면·코스 다음 목적지와 목록 fallback을 각각 고정한다.
- 375/1440 × 다크/라이트 4환경 실렌더를 확인했다. 375×812에서 점수·고칠 것·재제출 CTA가 모두 첫 스크롤 안이고 가로 overflow 0이다.
- `git diff --check` 통과, 콘텐츠 불가침 3파일 diff 0. Safari/WebKit·VoiceOver·실 Pages는 미검증이며 commit/push 없음.

## 질문/에스컬레이션
- 없음.

## 리뷰 기록
- 없음.
- 2026-09-15 PM: **r1 통과 → finished** — `docs/reviews/REV-TKT-154-r1.md`.
