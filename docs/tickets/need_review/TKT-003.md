# TKT-003

- status: need_review
- priority: P2
- version: v0.1.0
- title: Worker flow, sample service connectivity, and local test loop
- branch: `codex/v0.1.0-worker-node`

## Summary

Added a small worker loop that polls the gateway, claims queued tickets, probes the sample services, and completes tickets with a connectivity report.

## What Changed

- Added the `ion2-worker` Python worker scaffold.
- Wired the worker to the gateway API so it can claim and complete tickets.
- Added health and ping endpoints to the sample Python service and sample Spring service.
- Documented local service ports and the worker runtime contract.

## Validation

- Confirmed the worker depends on the gateway service metadata and health endpoints.
- Kept the sample services independent so they can be run and checked without the frontend.
- Kept the loop small enough to use as the first local ticket-execution path.

## Follow-up

- Orchestrator should run the worker against the gateway and both sample services to verify the end-to-end loop once the local runtime is available.
