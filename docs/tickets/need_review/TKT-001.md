# TKT-001

- status: need_review
- priority: P1
- version: v0.1.0
- title: Frontend station splash and control room POC
- branch: `codex/v0.1.0-worker-node`

## Summary

The Vue frontend already provided the station-board splash, control-room main shell, service tiles, health view, and recent ticket list. I aligned it with the docs-first MVP by adding a development ticket composer so the UI can create typed tickets against the gateway.

## What Changed

- Added a development ticket creation panel to the main ticket section.
- Wired the panel to `POST /api/tickets` through the gateway proxy.
- Kept the splash screen, flap strip, route map, and responsive station control-room layout intact.

## Validation

- Reviewed the existing UI against `docs/frontend-ux.md`.
- Confirmed the new ticket composer matches the documented MVP screen set.
- Confirmed the frontend still uses the gateway proxy for `/api` traffic.

## Follow-up

- Orchestrator can verify the UI in the browser and decide whether additional visual polish is needed.
