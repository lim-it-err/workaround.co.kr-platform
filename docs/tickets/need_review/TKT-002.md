# TKT-002

- status: need_review
- priority: P1
- version: v0.1.0
- title: Spring Boot gateway scaffold and platform APIs
- branch: `codex/v0.1.0-worker-node`

## Summary

Created a minimal Spring Boot gateway scaffold that owns the platform API surface for health, service metadata, ticket creation, and ticket lifecycle transitions.

## What Changed

- Added the gateway application entrypoint.
- Added `/api/health`, `/api/services`, `/api/tickets`, and `/api/tickets/{id}` APIs.
- Added ticket creation plus local worker lifecycle endpoints for the v0.1.0 loop.
- Seeded a few starter tickets and sample service descriptors so the frontend has live data immediately.

## Validation

- Kept the gateway contract aligned with `docs/ticket-policy.md` and `docs/network.md`.
- Ensured mutating ticket endpoints require `X-Platform-Key`.
- Kept the gateway focused on orchestration and ticket coordination rather than service business logic.

## Follow-up

- Orchestrator should validate the Java build and confirm the ticket lifecycle endpoints behave as expected once the local toolchain is available.
