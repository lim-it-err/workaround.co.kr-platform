# TKT-079

## 메타데이터

- 제목: advisor 디자인·프레임 통합 — 미션 화면을 모선 룩앤필로
- 우선순위: P1 (PM 보드·인박스 2026-09-09 승격 반영)
- 대상 버전: `v0.7.0`
- 상태: `need_review` (진입·복귀 및 전체 디자인·역명판 통합 완료, 2026-09-12)
- 문서 상태: `작성완료`
- 진행 판정: `검토 대기` (전체 완료 게이트 통과, 최종 판정은 PM)
- 담당: codex-1 화면 `[FE]`
- scope: `services/advisor/frontend/**`, `frontend/src/**`(진입 연결부만), `design/**`(기준선 문서 추가만)
- 의존: TKT-071(디자인 토큰), D-006(매니페스트), D-008

## 배경 (PO 지시)

advisor 는 2026-08-16 subtree 로 흡수됐다(D-008). PO: **"흡수하고 디자인이 다른 건 백로그로"** — advisor 프런트(Vue 3 프로토타입, 자체 스타일)는 모선의 지하철/노선 디자인 언어와 다르다. 기능 편입과 디자인 통합을 분리해, 디자인 차이는 이 티켓이 담당한다.

## 작업 내용

- `design/` 기준선 문서에 advisor 화면 진단 1장 추가 (현행 스타일 vs 모선 토큰 차이 목록).
- advisor 미션 화면(`services/advisor/frontend/src/modules/missions/` — 이식 단위로 동결돼 있음)을 모선 프레임에 연결:
  - 1안(기본): 미션 모듈을 모선 `frontend/src/modules/advisor/` 로 가져와 모선 토큰·내비를 적용.
  - 2안(과도기): advisor 프런트를 별도 빌드로 두고 `/advisor` 경로 서빙 + 모선 상단 프레임만 공유.
  - 선택 근거를 티켓에 기록하고 진행.
- 모선 노선도/게임·서비스 목록에 advisor 역 등록 (D-006 매니페스트).
- 콘텐츠 파일(`sampleContent.js` 등)은 **불가침** — 톤·문구 수정 금지 (advisor AGENTS.md 동결 계약).

## 완료 기준

- advisor 화면이 모선 디자인 토큰(색·타이포·간격)으로 렌더된다.
- 모선 내비에서 advisor 로 진입·복귀가 자연스럽다 (같은 오리진 경로).
- advisor 자체 검증 게이트(frontend 빌드 + `service/run.sh test`) 그린 유지.
- 편입 전후 화면 비교 기록을 히스토리에 남긴다.

## 질문/결정 기록

- (기록) 이 머신 환경 이슈: advisor frontend 빌드가 vite 5.4↔node 20.10 비호환(`util.styleText`)으로 실패한다. node ≥20.12 상향 또는 vite 고정 중 택일 필요 — TKT-080 에서 함께 처리.

### 2026-09-09 codex-1 — 1단계 정적 진입 연결 사전 점검

- 인박스 지시: TKT-100 need_review 뒤 `/advisor` 별도 정적 빌드 연결 및 노선도 A 개통을 먼저 수행. TKT-100의 need_review 상태를 확인했다.
- 현재 `.github/workflows/deploy-github-pages.yml`은 모선 `frontend`만 설치·빌드하고 `frontend/dist`만 Pages에 업로드한다. Advisor 설치·빌드·산출물 병합 단계가 없다.
- `infra/public-site/prepare-github-pages.mjs`는 모선 `index.html`을 루트 `404.html`로 복사한다. 별도 Advisor 앱의 깊은 경로를 선택하는 분기는 없다. `frontend/public`·`frontend/dist`에도 Advisor 산출물이 없음을 확인했다.
- Advisor router는 `createWebHistory(import.meta.env.BASE_URL)`이므로 project base를 포함한 `/workaround.co.kr-platform/advisor/` 빌드 및 하위 경로 새로고침 처리와 모선 복귀를 함께 검증해야 한다.
- 현재 티켓 scope에는 `.github/workflows/**`, `infra/**`, 모선 `frontend/vite.config.js`/`package.json`이 없다. 레인 지침도 `services/` 수정을 금지하므로, 기존 티켓의 `services/advisor/frontend/**` 허용과의 우선관계까지 PM이 명시해야 한다.
- **PM 질문**: ① Pages 빌드/산출물 병합/깊은 경로 처리를 codex-2의 명시적 티켓으로 배정할지, ② 이 티켓 및 레인 예외에 필요한 파일을 명시해 codex-1이 함께 수행할지 결정해 주세요. 현재 1단계에 별도 빌드를 지정한 인박스를 모듈 복사 방식으로 임의 대체하지 않았다.
- 해제 조건: 배포 연결 담당과 수정 범위가 확정되고, 같은 오리진 Advisor 진입 경로를 검증할 수 있는 빌드 계약이 마련될 것. 그 뒤 A의 upcoming 해제와 진입·복귀·375px·회귀 게이트를 수행한다.

## 2026-09-09 작업자 현황 (이력)

- 읽기 전용으로 워크플로·artifact 준비 스크립트·Advisor 설정/라우터·노선 정의·TKT-080을 확인했다. 코드·콘텐츠·배포 파일은 변경하지 않았다.
- A 노선의 `연결 준비 중` 표기는 유지했다. 구현 게이트 미실행 상태이므로 need_review로 전환하지 않는다.
- 커밋·push 없음. 외부 결정 대기인 이 티켓은 다음 루틴에서 건너뛰고 인박스의 다음 ready를 확인할 수 있다.


## PM 답변 (2026-09-10)

- 배포 파트는 **TKT-103 [INFRA]** 로 분리 발행 (workflow·prepare 에 advisor 빌드 포함, codex-2). 이 티켓의 scope 는 그대로 — 워크플로 파일을 건드리지 않는다.
- TKT-103 이 need_review 에 들어가면 착수 (의존성 완화 규칙). 1단계 = /advisor 링크 연결 + 노선도 A upcoming 해제.

## 2026-09-11 착수

- TKT-103의 need_review 인계와 nested-base/404 복구 계약을 확인해 기존 외부 대기를 해제했다.
- PM 지정 1단계로 노선 정의의 정적 진입 경로와 A 개통, Advisor 셸의 같은 오리진 복귀를 먼저 구현한다. 2안(별도 빌드)을 택하며 모선 미션 모듈 복사·iframe·워크플로 변경은 하지 않는다.
- 전체 디자인 토큰 통합은 1단계와 구분해 남긴다. 링크 연결만 검증한 상태에서 원래 티켓 전체를 need_review로 전환하지 않는다.

## 1단계 구현·검증 결과 (2026-09-11, codex-1)

### 구현

- 노선 단일 소스에 A의 `entryPath=/advisor/`, `render=static`, 정적 이용 상태를 등록하고 upcoming을 해제했다. JunctionMap은 project base를 보존한 실제 앵커를 렌더한다. SVG 클릭도 같은 경로로 이동하며 기존 앱 내부 이동은 유지한다.
- Advisor `src/app` 셸에 `← 환승 홀` 링크를 추가했다. 빌드 base의 마지막 `advisor/`만 제거하므로 Pages 중첩 경로에서도 같은 오리진으로 돌아간다. 단독 `/` 빌드에는 자기 자신을 향한 복귀 링크가 없다.
- 복귀 링크 추가 뒤 375px 메뉴에서 9px 넘침을 재현해 nav의 축소·줄바꿈·최대 폭을 보정했다. 미션 모듈·콘텐츠·저장 키·배포 설정은 수정하지 않았다.
- 디자인 진단과 단계 경계를 `design/advisor-frame-baseline-2026-09-11.md`에 기록했다. 별도 빌드를 유지해 동결된 미션 모듈을 복제하지 않았다.

### 검증

- [x] 모선 기본/Pages base build: 각각 37 modules. Advisor 기본/Pages nested-base build: 각각 97 modules. 기존 공유 dist를 덮지 않고 `/tmp/tkt079-check.CaQ8xb/`로 출력했다.
- [x] 모선 정적 라우팅·저장·여행 Node 테스트 3/3. Advisor unit 56/56(기존 44 + 복귀 base 12).
- [x] Advisor 기존 Chromium E2E 17/17. 포트 17179의 자체 서버를 strictPort/reuseExistingServer=false로 실행하고 종료했다.
- [x] `service/run.sh test`: JDK 21, 34/34 통과. 첫 샌드박스 실행은 Mockito self-attach 차단으로 실패했으며, 승인된 실행 환경에서 재실행해 통과했다. 백엔드 코드 수정 없음.
- [x] TKT-103 prepare를 임시 산출물에 적용, 모선·Advisor 각각 정적 참조 2개 검사 통과.
- [x] `services/advisor/frontend/checks/platform-navigation.mjs`로 병합 산출물을 검증했다. 375/1280px × dark/light 4조합에서 A 앵커 키보드 진입, 기내 모드 이동, 실제 404 응답 본문을 통한 새로고침 복구, 환승 홀 복귀·테마 유지, 데스크톱 SVG 진입이 통과했다. 페이지/본문 가로 overflow 0, pageerror 0, API 요청 0.
- [x] 변경 전후 캡처 비교: A의 점선·예정 상태가 개통으로 바뀌고, Advisor 메뉴 첫 항목에 복귀가 추가됐다. 모바일 메뉴는 2행으로 재배치되며 링크·본문 잘림 없음. 모선 다크/라이트, Advisor 고정 다크(두 OS 선호) 육안 확인.
- [x] `git diff --check`, 변경 문서 UTF-8 BOM/유효 UTF-8 검사 통과. 커밋·push·실배포 없음.

### 남은 작업 / 인계

- **전체 TKT-079는 완료 아님**: 모선 색·타이포·간격 토큰과 StationHeader를 Advisor 전체에 적용하는 작업이 남았다. `started/진행 가능`을 유지하며 전체 완료 게이트 이전에는 need_review로 옮기지 않는다.
- 실배포 및 비-Pages 호스트의 `/advisor/` 서빙은 이번 로컬 검증 대상이 아니다. 공개 반영은 PM의 TKT-103·079 변경 검토 및 commit/push 후 확인해야 한다.
- 재현: 두 앱을 Pages base로 빌드 → TKT-103 prepare에 `--dist <모선 산출물> --advisor-dist <Advisor 산출물>` 전달 → Advisor frontend에서 `node checks/platform-navigation.mjs <병합 산출물> <캡처 폴더>` 실행.
- 전후 캡처·결과 JSON·임시 빌드·E2E 자료: `/tmp/tkt079-check.CaQ8xb/`. 임시 자료이므로 정리 시 사라질 수 있다.

## 2단계 구현·검증 결과 (2026-09-12, codex-1)

### 구현과 범위

- 2안의 별도 빌드를 유지하며 Advisor 앱 셸에서 모선 `StationHeader.vue`를 재사용했다. A 노선 띠·역 코드·복귀 CTA와 기존 메뉴·닉네임 변경을 연결했다. 종전 브랜드 링크는 명시적인 `미션 목록` 메뉴로 유지했다. 기본 `/` 단독 빌드에서는 환승 홀 대신 미션 목록으로 복귀한다.
- `src/app/platform.css`에 모선 다크/라이트 색·폰트·카드 간격/반경과 A 코어/텍스트 토큰을 연결했다. 모선 전역 CSS를 가져오지 않고 기존 모듈 토큰을 매핑해 스타일 오염을 피했다. 모듈의 `--border`는 입력 경계 가독성을 위해 모선 `--line-strong`에 매핑했다.
- `workaround-theme`를 공유해 모선→Advisor→모선 이동, 토글·새로고침에서 선택을 유지한다. Advisor의 storage 이벤트 반영과 저장 차단 시 세션 내 테마 변경도 검증했다.
- 미션 컴포넌트 22개의 고정 색상/입력 표면을 테마 토큰으로 바꿨다. 코드 뷰어·제출 편집기는 라이트에서도 어두운 코드판/밝은 글자를 유지한다. 일반 입력·리뷰 인용·기내 카드·시즌 표시를 두 테마에 맞췄다.
- `modules/missions/data/**`, store, 게임 엔진 변경 없음. 변경된 22개 Vue의 템플릿·동작은 HEAD와 동일하며 SeasonPage의 색상 값 4개만 예외다. 콘텐츠 문구·저장/보상·API 계약, 워크플로·infra·다른 레인 파일은 수정하지 않았다.
- 빌드 시 모노레포의 StationHeader 소스가 필요하지만 모선 앱이나 gateway 실행은 필요 없다. 새 의존성·외부 폰트·이미지·네트워크 기능은 추가하지 않았다.

### 전체 완료 게이트

- [x] 모선 기본/Pages 빌드 각각 37 modules, Advisor 기본/Pages nested-base 빌드 각각 99 modules. 산출물은 `/tmp/tkt079-frame.Jiml46/{main,advisor}-final-{default,pages}/`에 격리했다.
- [x] 모선 정적 라우팅·로컬 저장·여행 콘텐츠 Node 테스트 3/3, Advisor unit 61/61(복귀 경로 12·테마/토큰 일치 5 포함).
- [x] Advisor Chromium E2E 21/21: 기존 17개 + 프레임 4조합. 14개 주요 경로와 프로젝트 여정·제출·닉네임 변경·단독 복귀·코드 대비를 검증했다. 375/1280px × dark/light에서 가로 overflow 0, 모바일 홈의 루트 글자 200%에서도 overflow 0.
- [x] 최초 전체 E2E에서 시즌 적립 관련 3개가 간헐 실패했다. trace에서 8/3 테스트 seed 대신 9/12 시즌 시작값이 남은 것을 확인했다. Vue 초기 홈의 routine 저장이 fixture 설정과 경쟁하므로 beforeEach가 홈 렌더 완료를 기다리게 보정했다. 보상 검증을 제거/완화하거나 앱 로직을 바꾸지 않았고 전체 21개를 재실행해 통과했다.
- [x] `cd services/advisor/service && ./run.sh test` — JDK 21, 34/34 통과(실패·오류·스킵 0). 백엔드 소스 변경 없음.
- [x] TKT-103 prepare를 임시 산출물에 실행해 모선/Advisor 정적 참조 각각 2개 검사 통과. `checks/platform-navigation.mjs`로 최종 병합 산출물의 A 키보드 진입·SVG 진입·테마 토글/복원·실제 HTTP 404 본문 새로고침 복구·같은 오리진 복귀를 4조합에서 검증했다. overflow/pageerror/API 요청 0.
- [x] 전후 육안 비교: 기존 ◆ 브랜드·고정 다크에서 A 역명판·모선 다크/라이트 짝으로 통일됐다. 375px에서 메뉴는 줄바꿈하고 복귀·테마 버튼이 노출된다. 1280px은 가로 메뉴를 유지한다. 기내·시즌·코드·닉네임 화면에서 대비와 잘림을 확인했다.
- [x] `git diff --check`, 변경 운영 문서 UTF-8 BOM/유효 UTF-8 검사. 커밋·push·배포 없음.

### 인계 / 재현

- 전체 TKT-079를 `need_review`로 인계한다. 위 1단계의 "남은 작업"은 이 절에서 완료됐으며, PM 최종 판정 및 TKT-103과 함께 공개 반영은 별도다.
- 검증 자료: `/tmp/tkt079-frame.Jiml46/`. 2단계 변경 전은 `before/after-advisor-*.png`(검증 도구 당시 파일명), 최종은 `after-final/after-advisor-*.png` 및 `after-results.json`, 주요 화면은 `e2e-final/`에 있다. 임시 자료는 정리 시 사라질 수 있다.
- 재현 명령: Advisor frontend에서 `npm run test:unit`, `npm run test:e2e`, 기본/nested-base build; 모선 Pages build → `node infra/public-site/prepare-github-pages.mjs --dist <모선 산출물> --advisor-dist <Advisor 산출물>` → Advisor frontend에서 `node checks/platform-navigation.mjs <병합 산출물> <캡처 폴더>`.
- 이번 E2E는 자체 strictPort 17179 서버로 실행했으며 종료됐다. Safari 실기, 실제 Pages 배포, 비-Pages 호스트의 `/advisor/` 서빙은 미검증이다. 기존 콘텐츠 카피와 모듈 내부 구조는 동결 계약대로 유지했다.
