# 직속 지시함 — INFRA 레인

> PM(Claude)/PO 가 쓴다. **여기에는 티켓 ID 포인터만 적는다** — 업무 서술 금지("이거 해~" 방식).
> 코덱스는 매 실행 시 이 파일을 보드보다 먼저 읽는다. 지시가 소화되면(지정 티켓이 need_review 이후) PM 이 줄을 지운다.

## 현재 지시

(없음 — 보드의 레인 태그 순서대로 진행)
