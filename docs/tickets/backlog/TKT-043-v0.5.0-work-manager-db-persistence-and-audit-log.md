문서 상태: 작성완료

# TKT-043

## 메타데이터

- 제목: Work Manager DB 저장소 전환과 감사 로그
- 우선순위: P2
- 대상 버전: `v0.5.0`
- 상태: `backlog`
- 문서 상태: `작성완료`
- 진행 판정: `v0.5.0 진행 시 가능`
- 소유자 유형: `worker`
- 권장 브랜치: `codex/tkt-043-work-manager-db-persistence`

## 목표

파일 기반으로 운영 중인 Work Manager 보드/활동 피드를 장기적으로 안정적인 DB 기반 저장소로 옮기고, 감사 로그를 남길 수 있는 구조를 마련한다.

## 작업 내용

- 현재 파일 기반 보드/티켓/피드 저장 경로를 정리한다.
- DB 전환 시 필요한 최소 스키마를 정의한다.
- 티켓 상태, 메타데이터, activity feed, command history 중 무엇을 우선 영속화할지 기본안을 정한다.
- 파일 기반 경로를 한 번에 버리지 말고 migration 또는 fallback 경로를 함께 남긴다.
- 감사 로그가 필요한 이벤트 범위를 정리한다.
  - 티켓 상태 전이
  - 우선순위 변경
  - 대상 버전 변경
  - command 실행
- 관련 문서와 운영 가이드를 갱신한다.

## 범위

- 포함: DB 저장소 설계, 영속화 범위 정의, 감사 로그 범위 정의, migration/fallback 기준, 문서 갱신
- 제외: 멀티유저 권한 체계 전체, 외부 SSO, 운영 대시보드 전체 재작성

## 완료 기준

- Work Manager 저장소의 파일 기반 현재 구조와 DB 전환 목표 구조가 정리돼 있다.
- 최소 영속화 범위와 감사 로그 범위가 문서화돼 있다.
- 파일 기반 fallback 또는 migration 경로가 남아 있다.

## 선행 조건

- `TKT-039` 와 `TKT-040` 의 UI/메타데이터 구조가 정리되어 있어야 한다.
- `TKT-042` 의 worker 가시화 / 우선순위 반응 설계와 충돌하지 않아야 한다.

## 질문/결정 기록

- 결정: 첫 단계의 Work Manager 는 파일 기반 저장으로 시작한다.
- 결정: 장기적으로는 DB 기반 저장소로 간다.
- 결정: 첫 DB 기준선은 gateway 안에서 관리 가능한 내장형 저장소를 우선 검토한다.
- 결정: `v0.5.0` 의 목표는 전면 치환보다 저장 인터페이스, 최소 스키마, migration/fallback 경로를 닫는 것이다.
- 제외: 별도 운영 DB 클러스터나 멀티노드 저장소 도입은 이번 티켓에 넣지 않는다.

## 선행 읽기

- `README.md`
- `docs/feature-definition.md`
- `docs/roadmap.md`
- `docs/ticket-policy.md`
- `docs/version-policy.md`
- `docs/tickets/finished/TKT-039-v0.4.0-work-manager-ready-lane-and-detail-panel.md`
- `docs/tickets/finished/TKT-040-v0.4.0-work-manager-ticket-meta-editor-and-version-header.md`
- `docs/tickets/finished/TKT-042-v0.5.0-work-manager-worker-visibility-and-priority-aware-pick.md`

## 작업자 산출물

- 브랜치 이름
- 저장소 구조 비교 요약
- 최소 DB 스키마 초안
- 감사 로그 이벤트 범위 요약
- migration/fallback 요약
- 검증 결과

## 검토 메모

- gateway 에 `gateway/data/work-manager-store.json` 파일 저장소와 audit log, target DB schema summary 를 추가했다.
- 검토자는 command/history/audit 가 파일에 남는지와 fallback 이 메모리-only 로 무너지지 않는지 확인하면 된다.
- 오케스트레이터 판정: 현재 구현은 파일 기반 audit/history 요약까지는 닫혔지만, ticket 목표에 있던 저장 인터페이스/adapter 경계와 migration 경로를 코드/문서 기준으로 아직 충분히 닫지 못했다. `backlog` 로 되돌린다.

## Notes

- 첫 단계의 파일 기반 저장을 섣불리 버리지 말고, 최소한 읽기/쓰기 계약과 migration 경로를 함께 남긴다.
- 기능 정의서 기준으로 `v0.4.0` 의 파일 기반 운영 화면을 깨지 않고, 그 위에 DB 기반 영속 저장과 감사 로그를 올리는 방향을 유지한다.
- worker 는 먼저 현재 파일 저장 계약을 표로 정리하고, 그 다음 최소 스키마와 adapter 경계를 코드/문서로 고정한다.
