문서 상태: 작성완료

# TKT-146 `[FE]` E2E 안정화 — 스플래시 타이밍 허용치·병렬 실행 내성

- 상태: started (2026-09-15, codex-1) · P2 · 담당: codex-1 · 의존: 없음. 브랜치 `codex/v0.7.0-tone`. **UX 1순위·[반박] 의무.** 제품 코드 무변경(테스트만).
- 근거: REV-TKT-140-r1 [제안] — 배치 병렬 실행에서 `splashTone.e2e.mjs` 10초 타이밍 테스트 1건 실패, 단독 6/6. AS-R009 §후속(스위트별 base 혼합 계약).
- scope: `frontend/src/splashTone.e2e.mjs`, 그 외 `frontend/src/**/*.e2e.mjs` 중 고정 sleep·wall-clock 의존 부분.

## 목표
PM 게이트가 여러 스위트를 동시에 돌려도 타이밍 때문에 red 가 나지 않게 한다. 방법은 구현자 판단(허용치 ±, 조건 대기로 교체, 가짜 타이머) — 단 **제품 동작 기준(첫 방문 10초·재방문 3초)을 느슨하게 단언하지는 않는다**(정착 시각 검증은 유지하되 CPU 경합 여유만).

## 완료 조건
1. [ ] 메인 E2E 전 스위트를 두 터미널에서 동시에 3회 실행 → 3/3 그린(실행 기록을 티켓에).
2. [ ] `splashTone.e2e.mjs` 단독 6/6, 병렬 6/6.
3. [ ] 고정 sleep 을 조건 대기로 바꾼 파일 목록과 이유 한 줄씩.
4. [ ] `frontend/src` 제품 코드 diff 0.

## 질문/에스컬레이션
- 없음.

## 리뷰 기록
- 없음.
