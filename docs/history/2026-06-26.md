문서 상태: 작성완료

# 2026-06-26

## heartbeat 작업 스케줄러 해제

- 10분마다 열리던 `cmd` 창의 원인을 확인했다.
- Windows Task Scheduler 에 등록된 `workaround-platform-orchestrator-heartbeat` 가 원인이었다.
- `workers/orchestrator-heartbeat/manage-heartbeat-task.ps1 -Action unregister` 로 작업을 해제했다.
- 별도로 보이던 `workaround-elevator-service-8003` 작업은 반복 작업이 아니라 1회성 잔재임을 확인했다.

## 블로그 공간 기획선 추가

- 사용자의 실제 목표를 반영해 `v0.6.0` 을 블로그 글쓰기 공간 라인으로 재정렬했다.
- `v0.6.0` 의 핵심은 `Blog District`, 공개 아카이브, 글 상세 읽기, `Writing Studio`, Markdown 저장 계약으로 정의했다.
- Flash Game 라인은 `v0.7.0` 으로 뒤로 미뤘다.

## 문서와 티켓 정리

- `docs/blog-writing-space-definition.md` 를 추가해 블로그 제품 목표, 정보 구조, 콘텐츠 모델, 제외 범위를 정리했다.
- `docs/manual-system.md` 를 추가해 작성자/운영/디자인 매뉴얼 체계를 정리했다.
- 디자이너 브리프 `design/orchestrator_review/2026-06-26-blog-writing-space-brief.md` 를 추가했다.
- 새 backlog 티켓을 추가했다.
  - `TKT-050`: 블로그 허브와 공개 아카이브/상세
  - `TKT-051`: 글쓰기 스튜디오와 초안/미리보기/발행
  - `TKT-052`: Markdown 저장 계약과 렌더링

## Blog District 디자인 기준선 추가

- 디자이너 핵심 산출물 6개를 `design/blog-district-ui-system-2026-06-26.md` 로 고정했다.
  - `Blog District` 진입 카드
  - 공개 아카이브 목록
  - 글 상세 typography 규칙
  - `Writing Studio` 편집/미리보기 레이아웃
  - 모바일 읽기/모바일 작성 우선순위
  - `draft / published / archived` 상태 badge 규칙
- 오케스트레이터가 바로 `TKT-050`, `TKT-051`, `TKT-052` 로 연결할 수 있도록 `design/orchestrator_review/2026-06-26-blog-district-design-handoff.md` 를 추가했다.
- 사용자 결정에 따라 이미지 업로드는 `v0.6.0` 첫 블로그 버전에서 제외하고, `v0.7.0` 후속 티켓으로 분리한다.

## 사용자 결정 반영과 worker 착수 기준 고정

- `TKT-049` 에 시간대별 수요 프리셋을 고정했다.
  - 출근 시간대는 1층 중심
  - 식사 시간대는 2층, 3층 중심
  - 저녁 시간대는 4층 중심
- `TKT-049` 의 car 수 변경 초기 배치는 수요 예측 재배치가 아니라 고정 분산으로 닫았다.
- `TKT-025` 택시 시뮬레이터 맵은 9구역으로 고정했다.
- `TKT-025` 는 실지도 API 를 선행조건으로 두지 않고, 필요하면 도시형 추상 9구역 지도로 먼저 닫도록 정리했다.
- `TKT-024` 는 정식 URL 라우터/게이트웨이 descriptor 없이 프런트 page 상태 확장으로 먼저 닫도록 정리했다.
- `TKT-042` 는 실제 worker 이름 또는 안정적인 worker ID 를 Started 가시화 기준으로 삼도록 정리했다.
- `TKT-043` 은 전면 DB 치환보다 adapter, 최소 스키마, migration/fallback 경계 고정이 먼저라는 기준을 남겼다.
- `TKT-050`, `TKT-051`, `TKT-052` 는 Blog District 디자인 기준선과 handoff 문서를 선행 읽기로 연결했다.
- `TKT-050` 은 메인 허브 직접 카드 진입으로, `TKT-051` 은 slug 자동 생성 + 수동 수정 허용으로, `TKT-052` 는 UI 메타 필드 분리 저장으로 기본 결정을 닫았다.

## v0.3.1 ~ v0.6.0 라이브 구현 묶음

- `frontend/src/App.vue`, `frontend/src/styles.css` 에 라이브 `Sim Hub`, `Taxi District Lab`, `Blog District`, `Public Archive`, `Post Detail`, `Writing Studio` 를 추가했다.
- `TKT-024`, `TKT-025` 범위로 메인 허브에서 `Sim Hub` 를 거쳐 Elevator / Taxi 로 분기되는 흐름과 9구역 택시 시뮬레이터 코어를 구현했다.
- `TKT-050`, `TKT-051`, `TKT-052` 범위로 localStorage 기반 글 모델, Markdown 렌더링, 공개 아카이브, 글 상세, draft/published/archived 전환이 가능한 `Writing Studio` 를 구현했다.
- `TKT-049` 범위로 엘리베이터 서비스 기본 이동 step 을 낮추고, car 수 2~6 조절 API, car 내부 승객 dot 표시, position easing 완화를 추가했다.
- `TKT-042`, `TKT-043` 범위로 gateway Work Manager 보드 응답에 `workerSummary`, `priorityPolicy`, `persistence` summary 를 추가하고, `gateway/data/work-manager-store.json` 파일 기반 audit/history 저장을 연결했다.
- 관련 backlog 티켓 `TKT-049`, `TKT-024`, `TKT-025`, `TKT-042`, `TKT-043`, `TKT-050`, `TKT-051`, `TKT-052` 는 구현 후 `need_review` 로 이동했다.

## 검증

- `tools/run-frontend-build.ps1` 빌드 성공
- `tools/run-gateway-tests.ps1` 테스트 성공
- 번들 Python 으로 `services/elevator-service/app.py` syntax check 통과
- 번들 Python 으로 엘리베이터 서비스를 띄운 뒤 `/api/state`, `/api/config` smoke 확인

