문서 상태: 작성완료

# 2026-09-09

## [claude] PM 복귀 — 대리 기간 전량 추인 + Advisor 진입 창 조치

- 추인: 초안 4건 정식 승격(TKT-095~098), etlpipe 은퇴(U-26), Pages 워크플로 트리거 트렁크 보정. 대리 일지 번복 0건.
- PO "workaround 에서 advisor 접속 창이 없다" → 사실 확인(TKT-079 백로그로 미연결). 즉시 조치: 노선도에 A 지선(upcoming '연결 준비 중') 추가 + line-a 토큰. 본편 연결은 TKT-079 P1 승격.

## [claude] Writing Studio 재검토 + 시뮬 묶음 지시 반영

- **무엇을**: PO 발주 2건 문서화 — ①Writing Studio 전면 재설계 스펙(`design/writing-studio-spec.md`: 크롬 4겹→바 1줄, primary 1개, 자동저장, 발행 시트, 서랍 드로어) + TKT-100 발행 ②시뮬 2종 단일 진입 결정 D-012("미스터리 트레인", 노선도 E·T 제거→S 지선, simhub=격납고) + TKT-101 발행. codex-1 큐 5건으로 갱신(097→100→079→101→082), 부관에 에디터 UX 서베이 브리프 투하.
- **왜**: PO "디자인이 조잡하고 기능을 모르겠다"(스튜디오), "재미 공간으로 묶고 노출 줄여라, 제목은 기믹"(시뮬). 스펙 없이 코덱스에 주면 조잡함이 재생산된다 — 원점 재검토는 스펙 먼저(U-17 원칙).
- **남은 위험**: TKT-100·101 이 App.vue 를 공유 — 순차 착수 명기했으나 충돌 시 리뷰에서 검출 필요. "미스터리 트레인" 명칭은 PO 거부권 있음(D-012 대안 기재).

## [claude] 리뷰 판정 2건 + Line V 종결

- **무엇을**: ①TKT-095 r1 **통과→finished** — 리뷰어 초안의 "구현 없음"은 루트 cwd 상대경로 grep 오검, PM 재검증(grep 5건·coverage 테스트 pass·build 그린·라이브 렌더로 접이식 3섹션 확인)으로 정정 ②TKT-096 **blocked 유지** — 리뷰어 로컬 8항목 재검증 채택, 남은 건 PO push 승인+Pages Settings ③TKT-084(Line V 스펙 [PM]) 종결 — 구현 티켓 전부 finished, voyage.js=단일 소스 ④리뷰어 지침에 "검증은 저장소 디렉터리에서" 규칙 추가, 리뷰어 인박스 정리.
- **왜**: PM 복귀 우선순위(수신함→need_review 판정) 집행. 같은 cwd 함정이 PM·리뷰어에서 이틀새 2번 재발 — 지침화.
- **남은 위험**: 096 실배포 확인은 push 뒤에만 가능. TKT-099(advisor 글쓰기 UX 관찰)는 TKT-079 연결 후가 자연스러워 대기.

## [claude] 사이트 공개(096 종결) + 3·4일차 미션 + 톤 재검토 목업

- **무엇을**: ①PO push 승인 집행 → Pages 활성화(gh api)·환경 브랜치 정책 추가 → **실배포 성공, 공개 URL 200** → TKT-096 finished ②voyage 3·4일차 상세 세션(체스키크룸로프 / 할슈타트→잘츠부르크) 추가 + coverage 테스트 4일치로 확장 + '어머니' 표현 잔존 제거, 커밋·push 로 라이브 반영 ③디자인 톤 전면 재검토(PO "너무 네모네모") — 원칙 4개(박스는 조작부에만·주인공 1·노선색은 선·영어 eyebrow 제거)를 구현과 분리된 목업 아티팩트로 제시, PO 승인 대기.
- **왜**: 여행 중 P0(공개 URL·다음 날 미션)이 최우선. 톤은 스펙·티켓 발행 전 목업 승인 절차(PO 지시: 실제 변경은 codex, 목업은 가짜 HTML 분리).
- **남은 위험**: 톤 원칙이 승인되면 기존 진행 중 티켓(100·101)의 스타일 지시와 정합 필요 — 스펙 발행 시 조정. Pages 환경 정책은 main 승격 시 재정리.

## [codex-1] TKT-098 Advisor 기내 콘텐츠 팩 구현

- **무엇을**: `/inflight` 오프라인 진입·이어보기, `/games` 전체 연습 카탈로그와 daily/practice 저장 분리, 기존 콘텐츠(사건 30일차 포함) 100개·신규 게임 24판·도메인 미션 6개(총 130개 신규 단위)를 Advisor 프런트에 추가하고 5개 내부 사양/완료 보고서를 남겼다.
- **왜**: 장거리 비행에서 외부 API 없이 3/10/30/60분 단위로 읽기·판단·추리·설계 훈련을 반복하고, 연습이 오늘 루틴·스트릭·시즌 보상을 중복 변경하지 않게 하기 위해서다.
- **검증/남은 위험**: unit 44/44, Chromium E2E 15/15, Vite build, diff-check, 375px 오버플로·육안/접근성 확인 통과. 공개 Pages와 service 백엔드는 범위 밖이며 커밋 없이 `need_review`로 넘겼다.

## [claude] 톤 목업 체계 전환(토큰 절약 모드) + Line V 구조 재검토 착수

- **무엇을**: ①전 화면 목업 제작을 PM→부관으로 이관: 톤 원칙 스펙(`design/tone-principles-2026-09-09.md`) + 기준 목업 저장소 사본 + **TKT-102**(codex-4 전담, scope=`frontend/public/mockups/**`, 정적 HTML 11개 → Pages 로 폰 검토) 발행 ②부관 인박스에 Line V 구조 서베이 브리프(노선=상설, 여행=정류장, PO 문제의식) 추가.
- **왜**: PO — PM 토큰 소진 임박, 목업은 부관에게. 목업 HTML 은 승인 후 구현 출발점으로 재사용해 이중 공수를 상쇄한다.
- **남은 위험**: 부관이 앱 코드에 손대면 블로커(scope 명시). 목업↔실화면 괴리는 [FE] 이식 시 리뷰로 잡는다.

## [claude] IA 전수 감사 ("Line V 패턴")

- **무엇을**: 전 페이지 요소를 "단일 인스턴스의 컨테이너 행세" 기준으로 감사(`design/ia-audit-2026-09-09.md`) — 전환 2(V 구조·재미 공간 S 통합), 예방 2(P·D), 정상 5. 메타 규칙을 D-013 으로 제안(노선=상설 공간·정류장=인스턴스·리트머스 "두 번째가 오면 이름이 거짓말이 되는가"). 부관에 독립 교차 감사 브리프 투하.
- **왜**: PO 지시 — Line V 에서 드러난 패턴의 전수 검토, 부관 병행.
- **남은 위험**: S 통합 판정이 확정되면 TKT-028(게임 공간 목록)의 화면 위치가 S 안으로 이동 — 착수 전이라 비용 없음. D-013 은 PO 확정 대기.

## [codex-1] TKT-097 여행·글쓰기 정적 UX 준비

- **무엇을**: GitHub Pages project base를 보존하는 Line V·Blog District·Writing Studio 라우팅, API 폴링 없는 정적 모드, 서버 전용 노선 비활성 안내를 추가했다. V03와 Writing Studio에는 같은 브라우저 저장 고지·손실 조건·3상태 저장 피드백을 넣고, 여행 메모와 블로그 초안을 함께 받는 JSON 백업을 제공했다.
- **왜**: API가 없는 Pages에서도 공유 여행 콘텐츠와 1·2일차 상세를 읽고, 여행 메모·블로그 초안·현재 편집 대상을 같은 브라우저에서 잃지 않게 하기 위해서다.
- **검증/남은 위험**: 관련 Node 테스트 3/3, 기본·Pages base Vite build, `git diff --check` 통과. 실제 Chromium에서 저장·복원·백업·정적 노선 차단과 375×812 다크/라이트 4화면 overflow 0을 확인했고 콘솔 `/api` 오류는 없었다. localStorage 특성상 브라우저 데이터 삭제·시크릿 모드·다른 기기에서는 복원되지 않으며, 화면에 이를 명시했다. 커밋 없이 `need_review`로 넘겼다.

## [codex-1] TKT-097 수동 재개 요청 확인·재검증

- **무엇을**: PO의 TKT-DRAFT-0338 계속 진행 요청에 따라 정식 티켓 TKT-097의 `need_review` 상태와 기존 구현을 확인하고, 관련 Node 테스트 3/3·기본 빌드·Pages base 빌드·diff-check를 다시 실행해 모두 통과했다.
- **왜**: 이미 구현 완료된 요청을 중복 구현하지 않고, 현재 워킹 트리에서도 정적 여행·글쓰기의 자동 검증 게이트가 유지되는지 확인했다.
- **남은 위험**: 이번 재검증에서는 브라우저 육안 검사를 반복하지 않았다. 코드·티켓 상태 변경 및 다른 티켓 착수 없이 PM 최종 판정 대기를 유지하며, 커밋·push는 하지 않았다.

## [codex-4] TKT-102 전 화면 톤 정적 목업

- **무엇을**: `frontend/public/mockups/`에 목차와 홈·여행 3종·블로그 3종·글쓰기·시뮬·작업실·런타임 등 화면 11종을 순수 HTML/CSS로 만들었다. 모든 화면에 실화면 아님 배너, 모바일 viewport, 데스크톱 max-width를 적용했다.
- **왜**: 실제 앱 코드를 바꾸기 전에 PO가 폰에서 원칙 4(조작부만 상자, 주인공 1개, 노선색은 선, 영어 간판 제거)를 전 화면 단위로 승인할 수 있게 하기 위해서다.
- **검증/남은 위험**: Vite build 통과, public/dist 각각 HTML 12개, 배너·외부 의존성·내부 링크를 정적 검사했다. 브라우저 육안 판정과 실화면 이식은 PM 검토 및 후속 FE 티켓 몫이며 앱 코드는 수정하지 않았다.

## [codex-1] TKT-100 글쓰기 스튜디오 재설계

- **무엇을**: 스튜디오 표면을 WritingStudio 컴포넌트로 분리해 바 1줄·단일 편집 영역·발행 시트·초안/보관 통합 서랍·같은 자리 미리보기로 변경했다. 로컬 저장 고지·도움말·백업과 기존 저장/상태 전환/렌더러 로직은 유지했다.
- **왜**: 첫 모바일 화면에서 바로 글을 쓰고, 메타데이터와 관리 기능은 필요할 때만 열도록 TKT-100 확정 스펙을 구현했다. 미승인 톤 목업(TKT-102)이나 다른 티켓 범위는 이식하지 않았다.
- **검증/남은 위험**: Chromium 회귀 8/8·정적 Node 테스트 3/3·기본/Pages build·diff/BOM 검사 통과. 375×812 다크/라이트에서 본문 즉시 입력·상태/primary 1곳·가로 overflow 0 및 모달 초점 순환을 확인해 need_review로 인계했다. 서버 저장·다중 탭·원격 공개는 기존 범위 밖이며, commit/push 없이 PM 판정 대기다.

## [codex-1] TKT-079 정적 진입 사전 점검 — 범위 결정 대기

- **무엇을**: 인박스 순서에 따라 TKT-079의 Pages 연결을 점검했다. 모선만 빌드·업로드되는 workflow와 루트 404 fallback, Advisor history base를 확인하고, 배포 연결 담당 및 레인 예외/수정 범위 질문을 티켓에 남겨 started/blocked로 기록했다.
- **왜**: 현재 산출물에 Advisor가 없고, 필요한 배포 파일은 티켓 scope 밖이다. A 노선의 upcoming만 해제하거나 모듈 복사로 임의 대체하면 지정된 별도 정적 빌드 계약과 어긋난다.
- **남은 위험**: PM의 codex-2 배포 작업 배정 또는 codex-1 수정 범위 명시가 필요하다. 이번 실행은 코드·콘텐츠·배포 변경이나 build/E2E 없이 사전 점검만 했으며 commit/push 없음. 다음 루틴은 외부 대기 티켓을 건너뛰는 규칙을 따른다.
