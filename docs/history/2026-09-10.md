문서 상태: 작성완료

# 2026-09-10

## [codex-1] TKT-101 미스터리 트레인 단일 진입

- **무엇을**: 노선도·행 목록의 E/T를 중립 S 지선 하나로 통합하고, `/sim`을 미스터리 트레인 헤더와 엘리베이터/택시 2카드로 정리했다. 통계·자기 해설·블로그 카드를 제거하고 시뮬에서 격납고로 돌아오는 내비를 연결했다.
- **왜**: D-012의 은근한 재미 공간·단일 진입 결정을 구현하면서 직접 URL과 시뮬 코어를 유지했다. 기존 TKT-097/100 및 다른 레인의 변경을 보존하고 지정 앱 파일 3개만 수정했다.
- **검증/남은 위험**: 기본/Pages build, Node 3/3, 글쓰기 E2E 8/8, 기존 dispatch 판정식의 Node 동등 검증, 4개 테마/폭 조합의 진입·복귀·직접 URL/새로고침·375px overflow 0·30초 화면 변화 및 diff/BOM 확인을 통과해 need_review로 인계했다. Pages 시뮬 차단은 TKT-097대로 유지한다. 검증은 격리 서비스/로컬 미리보기에서 했고 자료는 `/tmp/tkt101-check.op73rh/`; 임시 서버 종료, 커밋·push·배포 없음. 최종 판정은 PM 몫이다.

## [codex-1] TKT-082 택시 배차 누락 수정

- **무엇을**: 동일 구역 배정에서 빈 경로의 to-origin 차량이 idle로 돌아가 요청을 고립시키는 원인과, 완료 직후 재매칭 누락을 재현했다. 배차/운행 코어를 순수 함수로 추출해 0-hop pickup 전이 및 운행 후 재매칭을 추가하고 App의 수동/자동 호출·차량 추가 연결을 유지했다.
- **왜**: 유휴 택시가 있어도 큐가 줄지 않는 PO 보고를 고치면서 근접/정원 우선순위·경로·이동 틱·보상/패널티 계약을 보존하기 위해서다. TKT-074 지도·스타일 및 다른 기능은 수정하지 않았다.
- **검증/남은 위험**: 기존 코어 회귀 3건 실패를 먼저 확인한 뒤 수정 후 택시 unit 8/8(기존 정적 포함 11/11), 브라우저 E2E 5/5, 기본/Pages build, 글쓰기 E2E 8/8, 375px 다크/라이트 overflow 0·실제 30초 변화·diff/BOM 확인 통과. 6건 연속 수동 호출이 전부 완료됐고 자료는 `/tmp/tkt082-check.D6yZri/`에 남겼다. API는 fixture로 격리했으며 임시 서버 종료, 커밋·push·배포 없이 need_review로 인계했다. Pages 시뮬 제한 및 지도 후속 작업은 그대로다.

## [codex-1] TKT-DRAFT-0338 수동 재개 — TKT-097 현재 코드 재검증

- **무엇을**: 요청된 초안이 정식 TKT-097로 승격되어 need_review 상태임을 확인했다. 현재 워킹 트리에서 정적 라우팅·저장·여행 coverage 테스트 3/3, 기본/Pages base 빌드(36 modules), 글쓰기 브라우저 회귀 8/8 및 diff-check를 다시 통과했다.
- **왜**: 후속 글쓰기·시뮬 변경이 공존하는 상태에서도 초안 자동저장·편집 대상 복원·저장 실패 처리·통합 JSON 백업·API 요청 없음·375px 다크/라이트 overflow 0이 유지되는지 확인했다. 다크/라이트 스크린샷을 육안 확인했으며 자료는 `/tmp/tkt097-recheck.cdfyWp/`에 있다.
- **남은 위험**: 이번에는 V03 실브라우저와 실제 Pages 404 fallback을 재실행하지 않았다. 해당 검증은 기존 구현/리뷰 기록을 유지한다. 앱 코드·티켓 상태는 변경하지 않았으며 임시 서버를 종료했다. PM 최종 판정 대기, 다른 티켓 착수·커밋·push·배포 없음.

## [codex-1] TKT-074 착수 게이트 — 담당·완료 범위 PM 확인 대기

- **무엇을**: 인박스의 완료 건과 외부 대기 TKT-079를 건너뛴 뒤 TKT-074를 점검했다. 보드의 SIM ready/택시 지도 지정과 티켓·9월 8일 이력의 PM 직접 수행, 5개 페이지 완료 기준 충돌을 질문에 기록하고 backlog에서 blocked로 표시했다.
- **왜**: 담당을 임의로 가져오거나 지도만 구현한 뒤 다른 레인 작업까지 완료 처리하는 것을 막기 위해서다. 기존 스펙과 구현은 수정하지 않았으며, TKT-075도 S4 선행 조건이 풀리지 않아 착수하지 않았다.
- **남은 위험**: PM이 codex-1 택시 지도 단독 수행 여부와 수정 범위·완료 기준을 정렬해야 한다. 이번 실행은 문서 점검·질문/상태 기록만 수행했고 앱 변경·빌드·브라우저 테스트·커밋·push 없음.

## [claude] 08시 PM 판정 라운드 — 6건 판정 + 부관 리포트 3건 합산

- **무엇을**: ①need_review 6건 판정 — **통과 5**(082·097·100·101·098, 리뷰어 게이트 전건 + PM 재실행: 합산 build·unit 11/11·advisor 44/44 그린) / **반려 1**(102 — writing-studio.html 390px 제목 잘림 [중요], r2 요청). 커밋 2건(FE 묶음·advisor) ②REV-098 [중요]→TKT-104 발행, 079 blocked 답변→TKT-103 [INFRA] 분리 발행 ③부관 리포트 합산: Line V 구조 권고+IA 교차 감사→D-013 정밀화(**신규 발견: Advisor 시즌 단일 seasonStart 고정, 28일 후 적립 거절**), 에디터 서베이는 100 통과로 소급 불요 확인 ④수신함 4건 [x], 인박스 3개 갱신.
- **왜**: PO "지금 해줘" — 22:40 예약 라운드를 즉시 실행으로 대체.
- **남은 위험**: D-013 은 PO 확정 대기. Advisor 시즌 수명주기는 스펙 검토 후 티켓화(무단 발행 안 함). 102 r2 후 PO 폰 육안 승인 필요.

## [codex-4] TKT-102 r2 — 글쓰기 목업 모바일 제목 잘림 수정

- **무엇을**: 인박스 11번 재작업 포인터에 따라 `writing-studio.html`의 한 줄 제목 input을 줄바꿈 가능한 textarea로 바꿨다. 제목 문구·글꼴 크기는 유지하고 내용 기반 높이 및 2행 fallback을 적용했다. 다른 목업 11개 SHA-256 동일, 앱 코드 무접촉.
- **왜**: REV-TKT-102-r1의 [중요] 제목 내부 클리핑(390px에서 scrollWidth 367 > clientWidth 350)을 직접 재현했으며, 페이지 전체 overflow만 보는 검증으로는 이를 놓칠 수 있었다.
- **검증**: 기본 Vite build(36 modules)·dist 12개 바이트 일치·형식 요건·diff-check 통과. Chromium 375/390/540/541/1440px에서 제목 내부 가로/세로 잘림 0, 최초 화면 본문 입력, primary 1개, 긴 제목 확장·2행 fallback을 확인했다. 390px 및 데스크톱 스크린샷 육안 확인, 자료 `/private/tmp/tkt102-r2.muTOW1/`.
- **남은 위험**: Safari 실기 검증은 미수행(WebKit 미설치), fallback은 Chromium에서 CSS 강제 적용으로 확인했다. `need_review`로 재제출했으며 최종 판정·PO 폰 승인·커밋·배포는 PM 몫이다. 커밋·push 없음.

## [codex-1] TKT-104 기내 콘텐츠 필터 선택 상태 접근성

- **무엇을**: Inflight 시간 4개·취향 5개 버튼에 active 클래스와 동일한 조건의 aria-pressed를 추가하고, 기본 선택·전체 전환·Enter/Space·새로고침 복원·그룹 간 독립성을 검사하는 E2E 2건을 추가했다. 콘텐츠·추천/저장 로직·CSS는 변경하지 않았다.
- **왜**: REV-098의 중요 지적을 재현한 새 테스트 2건이 수정 전 실패했고, 선택 상태가 보조 기술에도 노출되도록 두 바인딩만 보완했다. 수정 후 E2E 17/17·unit 44/44·Advisor build 96 modules·모선 build 36 modules 및 diff-check를 통과했다.
- **남은 위험**: 375px에서 OS 선호 dark/light 각각 overflow 0·pageerror 0과 설정 영역 캡처를 확인했다(Advisor 고정 다크 유지). 실제 스크린리더 음성·실배포는 미검증이다. 테스트 서버 종료, 문서/BOM 확인 후 need_review로 인계했으며 커밋·push 없음. TKT-079는 TKT-103 need_review까지 대기한다.
