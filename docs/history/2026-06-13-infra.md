# 2026-06-13 - infra

## 요약

GitHub Releases / Actions 릴리스 자동화 기준선을 저장소에 추가하고, 관련 티켓을 `need_review` 로 올렸다.

## 변경 사항

- `.github/workflows/release.yml` 을 추가해 태그와 `VERSION` 일치 여부를 검증하는 기준선을 만들었다.
- `docs/release-automation.md` 에 현재 워크플로 파일과 확장 지점을 정리했다.
- `docs/tickets/need_review/TKT-015-infra-github-release-actions-baseline.md` 로 이동하고 검토 메모를 갱신했다.
- `docs/tickets/board.md` 와 티켓 인덱스를 현재 상태에 맞게 정리했다.

## 후속 고려 사항

- 실제 릴리스 노트 생성, 자산 업로드, GitHub Packages 발행은 아직 확장하지 않았다.
- 저장소 설정에서 릴리스 권한과 태그 생성 흐름을 확정하면 다음 단계로 이어갈 수 있다.
