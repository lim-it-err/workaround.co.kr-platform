문서 상태: 작성완료

# 2026-06-21

## UI-v0.4.0 로컬 상태 재확인

- `http://127.0.0.1:7000/`, `http://127.0.0.1:7000/test`, `http://127.0.0.1:8080/api/health`, `http://127.0.0.1:8003/health` 를 다시 확인했다.
- 세 경로 모두 로컬 프리뷰 기준 응답 `200` 이었고, 현재 포털 구조는 실사용 `/` 와 검수 `/test` 가 분리되어 있음을 재확인했다.
- `gateway /api/health` 는 `degraded` 였고, `ollama unavailable`, `sample-spring-service unavailable` 상태가 그대로 보였다.
- `elevator-service` 는 로컬 Python 경로로 직접 띄워 확인했다.

## UI-v0.5.0 프로토타입 레일 시작

- 새 가상 화면을 기존 `/test?view=...` 와 섞지 않기 위해 `/test/v0-5-0/...` 레일을 추가했다.
- `frontend/src/App.vue`, `frontend/src/styles.css` 에 `junction`, `taxi`, `ops`, `signals` 프로토타입 화면을 추가했다.
- `Taxi District Lab` 에는 구역 메쉬, 차량 상태, 리워드 보드를 더미 UX 로 구성했다.
- `Crew Board` 에는 worker 가시화와 우선순위 반응을 별도 승강장으로 분리했다.
- `Signal Room` 에는 `/`, `/test`, `/test/v0-5-0` 역할 분리를 명시했다.

## 문서 정리

- `design/v0.5.0-prototype-route-2026-06-21.md` 를 추가했다.
- `design/orchestrator_review/2026-06-21-v0.5.0-prototype-handoff.md` 를 추가했다.
- `design/README.md`, `design/ui-ux-rules.md`, `docs/frontend-ux.md`, `frontend/README.md` 에 새 프로토타입 레일 규칙을 반영했다.

## 남은 메모

- `VERSION` 파일은 여전히 `0.1.0` 이고, `docs/releases.md` 도 `v0.4.0` 공식 릴리스 완료로 닫혀 있지 않다.
- 따라서 오늘 확인은 "로컬 포털 구조와 가상 디자인 레일이 동작한다" 는 뜻이지, 공식 배포 메타데이터 정리가 끝났다는 뜻은 아니다.

