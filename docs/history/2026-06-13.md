# 2026-06-13

## Summary

Today's work focused on turning the repo into a docs-first personal platform monorepo and making the frontend and backend POC direction explicit.

## What Was Done

- Added `AGENTS.md` to define Codex as the docs-first project manager.
- Expanded `README.md` with the platform goal, repo layout, MVP scope, and architecture summary.
- Added core operating docs under `docs/`:
  - `architecture.md`
  - `network.md`
  - `ticket-policy.md`
  - `service-policy.md`
  - `ollama-policy.md`
  - `frontend-ux.md`
  - `agent-runtime.md`
- Added placeholder README files for the main monorepo folders so the layout is visible in the repo.
- Defined the frontend journey as:
  - a 10-second station-board splash screen,
  - flap or ticker humor messages,
  - an automatic transition into the main page,
  - a train-track style lower rail instead of a generic scroll footer.
- Defined the backend direction as a Spring Boot gateway, router, and ticket issuer with Redis Streams and worker-based execution.
- Clarified that heavy or LLM-like work should be routed through a separate worker path so RTX5070 Ollama can be stopped without breaking the platform.
- Created a local workspace copy at `C:\project\workaround.co.kr-platform` for manual inspection, because this machine did not have `git` installed at the start.
- Installed Git for Windows and created a real Git clone at `C:\project\workaround.co.kr-platform`.
- Updated the repo rules so every agent and worker must read docs and history before starting work, and must record completed work in history after finishing.
- Added the ticket operating system under `docs/tickets/` with these elements:
  - backlog, need_review, and finish folders
  - a central board file
  - orchestrator and worker role guides
  - initial prioritized backlog tickets with target versions
- Defined the repository ticket lifecycle so workers implement and test on branches, then return branch plus report for orchestrator review.
- Defined that orchestrator manages docs, ticket state, and validation, while the user keeps final merge authority.
- Added `TKT-005` for the next-version integrated local preview at `http://localhost:7000` so orchestrator can review major changes from one entrypoint.
- Documented the repository ticket priority scale as `P1` through `P5`, where `P1` is the highest priority and `P5` is the lowest.
- Re-aligned ticket priorities so the current backlog uses that scale consistently.
- Updated orchestrator workflow so it must watch `docs/tickets/need_review/` and confirm worker completion handoffs before review.
- Added `TKT-006` for infrastructure bootstrap and a single worker runtime.

## Notes

- The current docs intentionally separate orchestration from model inference.
- Sub-services stay language-independent and independently dockerized.
- The frontend should feel like a station control room, not a marketing landing page.
- The standard history file naming rule is now `docs/history/YYYY-MM-DD.md`.
- The standard repository ticket folders are `backlog`, `need_review`, and `finish`.
- The standard repository ticket priority scale is `P1` to `P5`.
