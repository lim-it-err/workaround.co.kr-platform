문서 상태: 작성완료

# 2026-07-06

## 요약

TKT-069 1차 디자인 반영으로 프런트 기본 테마를 다크로 고정하고, 스플래시 행선판 플랩을 아래→위 방향으로 재구성했다.

## 수행 내역

1. frontend/src/App.vue 에서 사용자 저장값이 없을 때 기본 테마가 항상 dark 로 시작하도록 초기값을 고정했다.
2. 스플래시 행선판을 상/하 분할 플랩 구조로 바꾸고, 하단 플랩이 먼저 접힌 뒤 상단으로 글자가 올라오는 애니메이션으로 조정했다.
3. prefers-reduced-motion 감지와 다시 재생 버튼을 연결해 모션 축소 환경에서는 결과만 표시하고, 일반 환경에서는 플랩을 다시 재생할 수 있게 했다.
4. 검증: tools/run-frontend-build.ps1 통과, tools/run-gateway-tests.ps1 통과, tools/check-docs-encoding.ps1 통과.

## 메모

- 이번 티켓은 사용자에게 빠르게 보이는 1차 반영만 다뤘다. A 골격/C 환승 홀/App.vue 분해는 후속 디자인 통합 티켓 범위로 남긴다.