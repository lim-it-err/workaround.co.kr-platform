문서 상태: 작성완료

# 2026-07-05

## 요약

Claude 오케스트레이터가 합류해 전체 계획을 재수립하고(`docs/replan-2026-07-05.md`), 갈라져 있던 git 기준선을 `origin/main` 위로 재정렬했다. 이후 UX 재작업 시안과 QA 조사를 에이전트에 착수시켰다.

## 사용자 결정

- 사이트 정체성: 개인 블로그 중심 + 실험 놀이터.
- 개발 상한 `v0.6.0`. 구현은 에이전트(워커) 위임, Codex도 참여 가능하게 티켓 유지.
- UX는 지하철 콘셉트 유지 + 전면 재작업. 시안 2~3개를 테스트 HTML로 검토. 플랩 연출은 더 매끄럽게.
- 오작동 수정 대상 지목: 엘리베이터 시뮬레이터, Work Manager, 블로그/글쓰기 스튜디오.
- 공개 순서: 블로그 먼저. 최종 목표는 호스팅.
- 코드 검토 채널은 PR, PR이 어려우면 태그.

## 수행 내역

1. 미커밋 `v0.3.1`/`v0.5.0`/`v0.6.0` 작업(39파일, +5,059/-362)을 `codex/local-snapshot-2026-07-05` 브랜치로 커밋하고 `snapshot-2026-07-05` 태그와 함께 push. `gateway/data/` 를 `.gitignore` 에 추가.
2. 로컬 `main` 을 `origin/main`(`328e8f8`)으로 fast-forward. 원격에는 `v0.2.0` 릴리스(버전 마커/릴리스 기록)와 PR #1(worker node), PR #2(엘리베이터 프리뷰)가 이미 머지되어 있었음을 확인 — 로컬 문서들이 이를 모르는 상태였다.
3. `codex/v0.6.0-line` 통합 브랜치 생성 후 스냅샷 머지(`c225f9e`). 충돌 24건 해소: 코드/운영 문서는 진화한 스냅샷 측 채택(원격 측은 v0.1.0 시절 영어 초기 문서였음), `VERSION` 은 `0.2.0`(원격) 유지, `docs/releases.md` 는 내용 병합(v0.2.0 릴리스 기록 반영, 버전 한눈에 보기에 v0.6.0=블로그/v0.7.0 추가).
4. origin 고유 픽스 보존 검증: gateway payload `new LinkedHashMap<>` 처리(현행 L419 존재), worker once-mode 중복 출력 제거(현행 코드에 반영됨).
5. 검증: `tools/run-frontend-build.ps1` 빌드 성공, `tools/run-gateway-tests.ps1` 테스트 3/3 통과.
6. 재수립 계획 `docs/replan-2026-07-05.md` 작성. `docs/roadmap.md` 갱신(배포 완료 `v0.2.0`, 개발 상한 `v0.6.0`, 1차 목표에 블로그 중심 확정 반영). `docs/history/README.md` 최신 항목 갱신.
7. 머지로 딸려온 원격 시절 잔재 정리: `docs/tickets/need_review/TKT-001/002/003.md`(영어 구버전, BOM 없음)는 로컬 `finished/` 에 최신본이 있어 중복 제거. `tools/check-docs-encoding.ps1` 110개 파일 통과.
8. 사용자 승인으로 `TKT-053`(블로그 이미지 업로드)을 `v0.7.0` 에서 `v0.6.0` 으로 편입. 티켓 파일명/메타데이터, `board.md`, `backlog/README.md`, `roadmap.md`, `releases.md`, `replan-2026-07-05.md` 일괄 갱신.
9. 사용자가 `gh` CLI 설치 완료(`C:\Program Files\GitHub CLI\gh.exe`). 인증(`gh auth login`)은 브라우저 절차라 사용자 진행 대기 중 — 인증되면 오케스트레이터가 기준선 PR을 직접 연다.
10. `docs/tickets/finished/TKT-031~034` 의 본문이 `UTF-8` 문서를 `Windows-1252` 로 잘못 해석한 모지바케 상태였음을 확인하고 본문을 정상 한글로 복구했다. `TKT-035` 는 같은 범위 점검 결과 본문/인코딩 모두 정상이라 유지했다.
11. 추가 점검으로 `TKT-01X` 범위를 확인해 `TKT-012`, `TKT-013`, `TKT-016`, `TKT-017`, `TKT-018` 이 같은 모지바케 상태였음을 확인하고 정상 한글로 복구했다. `TKT-010`, `TKT-011`, `TKT-014`, `TKT-015`, `TKT-019` 는 같은 범위 점검 결과 정상이라 유지했다.
12. `localhost:7000` 장애를 재현해 보니 서버 기동 자체보다 프런트 런타임 오류가 문제였고, `frontend/src/App.vue` 의 택시 시뮬레이션 경로에서 빠진 `clamp()` 유틸 때문에 `ReferenceError` 가 반복되고 있음을 확인했다. 누락된 함수를 복구해 메인 허브 전환 이후 콘솔 오류를 제거했다.

## QA 티켓화와 ready 착수 대기열 도입

- 디자이너 에이전트가 지하철 콘셉트 유지 HTML 시안 3종을 `design/mockups/2026-07-05/`(A 서울 사인 정제 / B 종이 아카이브 / C 야간 노선도)로 완성했다. 사용자 검토 대기.
- QA 에이전트 3종이 엘리베이터(13건)/Work Manager(14건)/블로그(12건) = 39건을 코드 레벨로 조사했다. 사용자 지목 오작동의 원인을 확인: 엘리베이터 warp(폴링 기반 시뮬 + car-count 재초기화), Work Manager 401 토큰 미정리, 블로그 편집 유실/발행 무결성.
- 39건을 실행 지시서 티켓 11개(TKT-054~064)로 변환해 `backlog` 에 등록했다.
  - v0.3.2 엘리베이터: `TKT-058`(시뮬 코어 P1, car-count/warp/배차), `TKT-059`(프런트 모션 P2).
  - v0.4.1 Work Manager: `TKT-061`(인증 만료 UX P1), `TKT-062`(전이 흐름), `TKT-063`(저장소 정합), `TKT-064`(동시성+테스트).
  - chore 보안: `TKT-060`(인증 하드닝 P1 — 세션 토큰 공개 유출/XFF 잠금 우회/해시).
  - v0.6.0 블로그: `TKT-054`(저장/발행 무결성 P1), `TKT-055`(공개 라우팅/딥링크 P1), `TKT-056`(아카이브 상태/배지), `TKT-057`(마크다운 렌더러+XSS 가드).
- 티켓 흐름을 `inbox -> backlog -> ready -> started -> need_review -> finished` 로 확정하고 `docs/tickets/ready/` 를 실제 도입했다. 정책 문서(ticket-policy, tickets/README, orchestrator, worker, feature-definition, board)를 ready 흐름으로 개정했다.
- `docs/discord-claude-codex-bridge.md` 의 `codex-ready-worker-watch`(10분 heartbeat) 프롬프트를 ready 도입 확정판으로 갱신했다. Codex 워커는 `ready` 에서만 티켓을 집는다.
- 보안 hotfix `TKT-060` 을 가장 먼저 `ready` 로 승격해 heartbeat 워커가 우선 착수하게 했다.

## 다음 액션

- 사용자 시안 선택(A/B/C 또는 조합) 확정 → 디자인 시스템 문서 갱신 → UI 재구현 티켓.
- Codex heartbeat 가동 시 `TKT-060`(보안) 먼저 착수. 이후 P1 티켓(`TKT-058`/`TKT-054`/`TKT-055`/`TKT-061`)을 순차로 `ready` 승격.
- 보드 정리(TKT-018 재판정, started/README 정리)와 기준선 PR(`codex/v0.6.0-line` → main)은 `gh` 인증 후.

## Discord-Claude-Codex 브리지 선행 설계

- 사용자가 밖에 있을 때 Discord에서 요청을 남기고, Claude가 PM/디자인/오케스트레이터 관점으로 티켓 초안을 만들며, Codex가 로컬 저장소에서 timer로 이를 가져가는 운영안을 설계했다.
- 첫 버전은 별도 OpenAI/Anthropic API 브리지를 만들지 않고 Claude Code Discord Channel, Claude hook, Codex hook, Codex thread automation을 조합하는 파일 기반 큐로 잡았다.
- 새 설계 문서 `docs/discord-claude-codex-bridge.md` 를 추가했다.
- 외부 인입용 `docs/tickets/inbox/README.md` 와 `docs/tickets/inbox/claude/README.md` 를 추가하고, `docs/tickets/README.md` 에 `inbox -> backlog/ready -> started -> need_review -> finished` 흐름을 반영했다.
- `docs/agent-runtime.md` 에 Discord-Claude-Codex 브리지 책임 경계를 추가했다.
- Redis는 v0 필수 요소가 아니며, 여러 worker, 중복 처리, 실시간 Work Manager UI가 필요해질 때 v1 확장으로 도입하는 것으로 정리했다.

## PR 초안 메모 (codex/v0.6.0-line → main)

- 제목: `[infra] realign baseline: merge local v0.3.1-v0.6.0 line onto origin/main`
- 본문 요지: 로컬/원격 분기 해소를 위한 기준선 통합. 스냅샷 안전 확보 → origin/main 재정렬 머지 → 빌드/테스트 검증 → 릴리스 문서 정합화. Target Version: `infra`(기준선 정리). one-version-one-feature 예외: 이미 얽혀 있던 다중 버전 스냅샷의 1회성 통합이며 사유를 본문에 명시한다.
- 검증: frontend build 성공, gateway tests 3/3, origin 고유 픽스 2건 보존 확인.
- 태그: `snapshot-2026-07-05`(머지 전 로컬 상태), `baseline-2026-07-05`(통합 후 기준선).

## 실험 놀이터 확장: 발견 노선 / 취향 노선 설계

- 사용자 요청으로 실험 놀이터 기능 2종을 설계했다.
  - 발견 노선(`Line D`, `v0.8.0`): 매일 랜덤 주제 1개 + 3방향(더 깊이/더 넓게/뜻밖의 각도) depth 3. depth 0~1 정적(주제 100개) + depth 2~3 Claude API 온디맨드+캐싱 하이브리드로 토큰 절약. `docs/discovery-line-definition.md`.
  - 취향 노선(`Line P`, `v0.9.0`): 10 A/B 선택 → 규칙 기반 스코어링 → 와인 1 + 커피 1 추천. 2^10 조합 폭발 없이 와인 24·커피 24 프로파일 최근접 매칭, 런타임 토큰 0. `docs/taste-line-definition.md`.
- `docs/feature-definition.md`(실험 놀이터 확장 섹션)와 `docs/roadmap.md`(v0.8.0/v0.9.0)에 두 노선을 추가했다. 티켓 `TKT-065`(발견 UI+정적), `TKT-066`(발견 depth API), `TKT-067`(취향)을 backlog 에 등록하고 board/backlog README 를 갱신했다.
- 정적 데이터(주제 100개, 와인 24·커피 24)는 에이전트로 사전 생성 중(`frontend/src/data/`).
- 디자인 방향 확정: 시안 A 골격 + 시안 C 노선도(환승 홀) + 다크 기본 + 플랩 아래→위. `design/orchestrator_review/2026-07-05-ux-overhaul-mockups.md` 에 기록.
- Codex heartbeat 가 `TKT-060`(보안)을 완료해 `need_review` 로 handoff. `codex/tkt-060-work-manager-auth-hardening` 브랜치에 커밋/push(gateway tests 7 통과). 오케스트레이터 검토 대기.
- 티켓/문서의 버전 하드코딩("v0.4.0과 v0.5.0 범위")을 "기능 페이지" 로 일반화. Discord 브리지는 Remote Control 로 대체(전환 노트 반영).
