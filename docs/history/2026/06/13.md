# 2026-06-13

## Summary

Today's work focused on turning the repo into a docs-first personal platform monorepo and making the frontend/backend POC direction explicit.

## What Was Done

- Added `AGENTS.md` to define Codex as the docs-first project manager.
- Expanded `README.md` with the platform goal, repo layout, MVP scope, and architecture summary.
- Added core operating docs under `docs/`:
  - `architecture.md`
  - `network.md`
  - `ticket-policy.md`
  - `service-policy.md`
  - `ollama-policy.md`
  - `frontend-ux.md`
  - `agent-runtime.md`
- Added placeholder README files for the main monorepo folders so the layout is visible in the repo.
- Defined the frontend journey as:
  - a 10-second station-board splash screen,
  - flap/ticker humor messages,
  - an automatic transition into the main page,
  - a train-track style lower rail instead of a generic scroll footer.
- Defined the backend direction as a Spring Boot gateway/router/ticket issuer with Redis Streams and worker-based execution.
- Clarified that heavy or LLM-like work should be routed through a separate worker path so RTX5070 Ollama can be stopped without breaking the platform.
- Created a local workspace copy at `C:\project\workaround.co.kr-platform` for manual inspection, because this machine did not have `git` installed.

## Notes

- The current docs intentionally separate orchestration from model inference.
- Sub-services stay language-independent and independently dockerized.
- The frontend should feel like a station control room, not a marketing landing page.
