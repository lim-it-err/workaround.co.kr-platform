문서 상태: 작성완료

# 2026-08-17 작업 기록

## [codex-1] TKT-054 Writing Studio 저장/발행 무결성

- **무엇을**: dirty 표시와 800ms autosave/이탈 guard, 제목만 초안 저장, 발행 상태·최초 발행일 보존, 명시적 발행 취소, 공개 글 편집 진입점, 영속 `slugLocked`를 구현했다.
- **왜**: 편집 이탈 유실, 저장만으로 공개 글이 내려가는 상태 역전, 발행일 소실, 수동 slug 덮어쓰기를 한 저장 계약에서 제거하기 위해서다.
- **검증/남은 위험**: 발행→저장·발행 취소 왕복·autosave 이동/새로고침 복원·제목만 초안·수동 slug·build·양 테마·375px 오버플로 0을 통과했다. 다중 탭/저장소 손상 복구는 티켓 범위 밖 후속 과제다.

## [codex-1] TKT-061 Work Manager 인증 세션 재잠금

- **무엇을**: 인증 응답의 `expiresAt` 저장·복원, TTL 타이머와 요청 전 만료 검사, 401 공통 처리, 토큰/저장소 동시 정리, 잠금 시 실행 버튼 비활성화를 구현했다.
- **왜**: 만료된 토큰이 localStorage 에 남아 UI는 unlocked 로 보이지만 모든 실행 요청이 401 로 실패하던 체감 오작동을 제거하기 위해서다.
- **검증/남은 위험**: 빌드와 격리 모의 API에서 만료값 재로드·TTL 자동 잠금·401 재잠금·재인증 복구·양 테마를 확인했다. 실제 gateway 기본 30분 TTL 통합 재현은 리뷰 단계의 선택 검증으로 남겼다.

## [codex-1] TKT-055 블로그 공개 라우팅/딥링크

- **무엇을**: `/blog` 목록과 `/blog/:slug` 상세 URL, pushState/popstate 기반 앞뒤 이동, 초기 딥링크 복원, 없는·비공개 slug의 전용 404를 구현했다.
- **왜**: 상세 새로고침이나 링크 공유 시 splash로 이탈하고, 잘못된 주소가 첫 공개 글을 대신 보여주던 공개 라우팅 오류를 제거하기 위해서다.
- **검증/남은 위험**: build, 새 탭·새로고침·뒤로/앞으로, 없는·draft·archived slug, 양 테마 375px overflow 0을 통과했다. 실제 정적 호스트의 `/blog/* -> index.html` rewrite는 티켓 범위 밖 배포 설정으로 남는다.

## [codex-1] TKT-062 착수 게이트 차단 기록

- **무엇을**: `[FE]` 배정과 실제 완료 범위를 대조해 gateway 전이 매트릭스·테스트 수정이 화면 레인 금지 범위임을 확인하고 티켓을 `blocked/진행 불가`로 기록했다.
- **왜**: gateway 필수 변경을 누락한 채 UI만 구현하거나, 미확정인 서버 전이/localStorage 제안 계약을 임의 선택하면 티켓 완료 조건과 PM 권한을 침해하기 때문이다.
- **남은 위험**: PM이 BE 범위를 분리·재배정하고 `Backlog -> Ready`의 최종 UI 계약을 확정하기 전까지 TKT-062 전체 완료는 불가능하다.

## [codex-1] TKT-083 엘리베이터 수직 단면 재설계

- **무엇을**: 23층 수직 단면에서 car 연속 위치·층별 대기 도트가 직접 보이도록 재구성하고, 한산·보통·혼잡 프리셋, 실시간 지표, 최근 운행, 층 클릭 승객 추가, 모바일 우선 배치를 구현했다.
- **왜**: 기존 수치 타일과 목록형 표는 자동 운행 중에도 정지 화면처럼 보여, R1 스펙의 핵심인 “살아 움직이는 건물 단면”과 30초 관측 가능성을 충족하지 못했다.
- **남은 위험**: 프런트 게이트는 통과했지만 현재 다른 레인의 gateway 변경분이 Spring 생성자 오류로 기동되지 않아, 실제 서비스 검증은 임시 로컬 프록시를 거쳤다. gateway 통합 회귀는 플랫폼 레인 확인이 필요하다.

## [codex-1] TKT-088 착수 게이트 차단 기록

- **무엇을**: TKT-088의 `ready` 착수 전 원문·연관 티켓·데이터·기준 문서를 재검토하고, 순환 의존과 누락 스펙을 티켓 질문 및 보드에 `blocked/진행 불가`로 기록했다. 코드 변경은 하지 않았다.
- **왜**: TKT-088은 TKT-091 토큰을 완료 게이트로 요구하지만 TKT-091은 TKT-088~090을 선행으로 두며, TKT-084의 산출물 `design/voyage-line-spec.md`도 아직 존재하지 않아 임의 구현이 금지된다.
- **남은 위험**: PM이 의존 순서와 화면 스펙을 확정하기 전까지 Line V 준비 화면을 착수할 수 없다.

## [claude] 상단 크롬 슬림화 + 노선도 한 화면 + SVG 클릭 이동 (PO 피드백 3건 반영)

- **무엇을**: ①**슬림 상단 바** — 거대 타이틀+부제+버튼 3단 헤더를 레퍼런스(구 블로그 헤더)식 한 줄 바(원형 노선 배지 + 작은 타이틀 + 우측 아이콘 버튼, 하단 노선색 3px 라인)로 교체. 부제("기능을 직접 실행하지 않고…") 삭제. 전 페이지 공통, 페이지별 노선색 자동(TOPBAR_LINES 맵) ②**노선도 한 화면** — SVG `max-height: min(58vh, 520px)` 캡 + 크롬 축소로 1280×800에서 지도 하단 666px(<뷰포트) 실측 ③안쪽 페이지의 중복 상태 줄(wayfinding-bar) 제거 — 위치는 상단 바가, 상태는 역명판 칩이 이미 담당 ④**노선도 라벨/배지 클릭 = 페이지 이동** — 지선 g 클릭, 본선 정차역 클릭(공개 아카이브→blogArchive, 글 상세→bloghub, Studio→writingStudio), upcoming 은 비활성. hover 시 라벨 노랑.
- **검증**: 빌드 통과, 1280×800에서 지도 전체 노출 실측, SVG 'Elevator Station' 라벨 클릭 → 승강장 이동 확인. 모바일(375px) 회귀 없음(행 목록 우선 유지).
- **참고**: 검증 중 "스플래시 정지"는 숨겨진 브라우저 패널의 rAF 스로틀 — 코드 버그 아님.
- **남은 위험**: 안쪽 페이지 상단은 이제 상단 바 + 역명판 2겹 — PO "두번째 페이지 양식" 후속 피드백 대기. wayfinding-bar CSS 는 미사용 잔존(S5 정리).

## [claude] 역명판 prev 클릭 이동 + TKT-074 격상 (PO 지시)

- **무엇을**: StationHeader 의 "← 환승 홀"(prev)을 버튼화 — 클릭 시 환승 홀 이동(exit 이벤트, hover 노랑). TKT-074(S4)를 PM 직접 수행으로 전환하고 PO 요구를 티켓에 반영: **기능 명료화**(3초 룰: 뭐 하는 화면/무슨 일/뭘 할 수 있나) + **택시 3×3 지도형 그리드**(수요 톤·차량 마커).
- **왜**: PO — "각 기능별로 너무 뭐가 뭔지 모르겠어, 너가 할래?" / "택시는 최소한 지도는 있어야". FE 코덱스 레인과 충돌하지 않게 보드에서 TKT-074 를 `[PM]` 으로 표기.
- **남은 위험**: S4 본 구현은 다음 세션 착수. FE 레인은 S3(블로그)부터 돌게 유지.

## [claude] 오전 심의 집행 + 시뮬 재설계 스펙 2건 (2026-08-18 오전)

- **무엇을**: 상황실 답변 집행 — D-005/006/009 확정(J4), P1~P3 통일(J8), 낡은 사본 삭제(J5), voc-qa 배포 타깃 `.100` docker 전환(PO 지시). R1 승인으로 **`design/sim-elevator-spec.md`**(수직 단면 뷰·자동 수요·"방치 30초" 게이트)와 **`design/sim-taxi-spec.md`**(버그 먼저 → 3×3 지도·차량 마커) 작성, TKT-083/074/082 를 [SIM] ready 로. R2 승인으로 S3 에 연재 뷰 수용 기준 추가.
- **왜**: [SIM] 레인이 스펙 없이는 못 움직이는 구조 — 스펙이 나온 순간 레인 3장이 즉시 착수 가능해졌다.
- **남은 위험**: work/runtime 명료화는 [FE] 후속 분할 필요. J6(AirportTycoon 웹 편입)은 성능 이전에 **Godot 4 C# 웹 익스포트 미지원**이 결정적 — PO 답변에 반영.

## [codex-2] TKT-085 Cloudflare/Caddy 공개 ingress 구현, 검증 차단

- **무엇을**: `local`/`cloudflare` compose 프로필, Origin CA TLS Caddy 라우팅(`/api/*`→gateway, `/arcade/*`→정적), 두 zone A 레코드 DDNS 스크립트, PO 수작업 체크리스트와 비밀값 제외 규칙을 추가했다.
- **왜**: Cloudflare 프록시 뒤에서 집 IP를 직접 노출하지 않고, 공개 LLM 요청도 D-009에 따라 반드시 gateway를 경유하도록 공개 진입점을 고정하기 위해서다.
- **남은 위험**: compose 렌더링과 JDK 21 gateway 테스트는 통과했지만 `caddy:2.9-alpine` pull이 두 차례 정지해 실제 Caddy 기동·Host 헤더 curl 게이트는 미완료다. registry 접근 복구 후 같은 로컬 프로필을 재검증해야 한다.

## [codex-2] TKT-085 Caddy pull 차단 재검증

- **무엇을**: Docker daemon `24.0.2`와 Docker Hub registry endpoint 도달(HTTP 401)을 다시 확인하고 `caddy:2.9-alpine` pull을 재시도했다.
- **왜**: 이전 실행의 유일한 차단 조건이 해제됐는지 확인해 로컬 compose/Host 헤더 curl 완료 게이트를 이어가기 위해서다.
- **남은 위험**: host HTTPS는 정상이지만 Docker engine pull은 다시 약 50초간 무응답이었다. 타 작업 컨테이너를 보호하기 위해 Docker Desktop 강제 재시작은 하지 않았고 티켓은 `blocked`를 유지한다.

## [codex-2] TKT-087 advisor Haiku provider 구현, 실 호출 대기

- **무엇을**: advisor `claude` 프로필의 출제·리뷰·채팅을 Haiku로 통일하고, Anthropic 호출 실패 시 타입이 맞는 mock 응답으로 폴백하는 role route를 추가했다. 환경변수 프로필 기동과 모델 문서도 함께 정렬했다.
- **왜**: PO의 M2 Haiku 연동 지시를 따르면서 외부 API 장애가 학습 full cycle 전체를 중단하지 않게 하기 위해서다. 응답 계약을 바꾸지 않아 기존 "모델 초안" 표기도 유지한다.
- **검증/남은 위험**: JDK 21 `service/run.sh test` 34건은 모두 통과했다. 실행 환경에 `ANTHROPIC_API_KEY`가 없어 실 Haiku full cycle 1회만 미완료이며, PO 키 주입 전까지 티켓은 `blocked`다.

## [codex-1] TKT-056 블로그 보관 복원·상태 배지

- **무엇을**: Studio에 Archive Shelf를 추가해 보관 글을 목록으로 노출하고 초안/공개 복원 액션을 연결했으며, draft/published/archived 배지를 슬레이트/그린/브론즈로 분리했다.
- **왜**: `archived`가 되돌릴 수 없는 사실상 삭제로 작동하고 세 상태가 같은 회색으로 보여, 디자인 §6의 보관 의미와 상태 식별 규칙을 위반하던 문제를 닫기 위해서다.
- **검증/남은 위험**: build, 보관→초안·공개 복원, 공개 URL 재노출, 양 테마 색상, 375px Studio overflow 0, 콘솔 error 0을 통과했다. 영구 삭제는 의도적으로 제외했고 localStorage 다중 탭/서버 저장은 후속 범위다.

## [codex-1] TKT-057 마크다운 충실도·XSS 가드

- **무엇을**: 중첩·순서 목록, 문단 병합, code-span 우선 처리, allowlist 기반 링크/이미지를 추가하고 태그 key와 글 ID 생성을 고유한 방식으로 정리했다.
- **왜**: Markdown-first 글쓰기에서 기본 문법이 깨지던 문제를 고치면서도, escape-before-tokenize와 raw HTML 차단이라는 기존 XSS 불변식을 유지하기 위해서다.
- **검증/남은 위험**: build, 마크다운 fidelity 샘플, `javascript:`/raw HTML XSS paste, 콘솔 경고 0, 양 테마 375px overflow 0을 통과했다. 서버 공유 전 검증된 sanitizer 도입과 완전한 CommonMark 호환은 후속 범위다.

## [codex-1] TKT-073 블로그 공개면·StatusBadge 재구현

- **무엇을**: draft/published/archived를 슬레이트/그린/브론즈로 구분하는 공용 `StatusBadge`를 만들고, 블로그 허브·아카이브·상세를 `blog-primary`/`archive-item`/`post-detail` 문서형 구조로 재구현했다. `series:<표시명>` 태그가 있는 공개 글은 연재 섹션으로 묶는다.
- **왜**: S3 시안의 문서 계층과 720px 읽기 폭을 적용하고, 공개면에는 published만 보인다는 상태 경계를 UI와 데이터 선택 모두에서 고정하기 위해서다.
- **검증/남은 위험**: 빌드, 양 테마, 375px overflow 0, 상세 720px/상단 5px, 공개면 비공개 배지 0건, Studio 3상태 색, Line V 연재 런타임, 콘솔 error/warning 0을 통과했다. 실제 Line V 콘텐츠는 TKT-090, Studio 흐름 마감은 TKT-076 범위다.

## [codex-1] TKT-091 Line V 노선 등록

- **무엇을**: 스틸 그레이-블루 V 토큰을 양 테마에 추가하고, 남서 지선·환승 홀 행/SVG 라벨·Voyage 라우팅과 V01 준비 헤더를 연결했다.
- **왜**: TKT-088~090이 공통으로 의존하는 노선 토큰과 진입 경로를 먼저 확립해 순환 없이 화면 구현을 이어갈 수 있게 하기 위해서다.
- **검증/남은 위험**: build, 행/SVG 클릭 이동, 양 테마 색 구분, 375px 오버플로 0, console error/warning 0을 통과했다. 실제 체크리스트·일정·기록 본문은 후속 TKT-088~090 범위다.
