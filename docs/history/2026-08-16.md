문서 상태: 작성완료

# 2026-08-16 작업 기록

## [claude] 트렁크 확정 — `codex/v0.6.0-line` (D-002, PO 승인)

- **무엇을**: PO 승인("그렇게 해")으로 이 브랜치를 공식 트렁크로 확정하고 체크아웃했다. `main`(v0.2.0, 2026-06-18 정지)은 화석으로 표기. 앞선 세션에서 main 기준으로 잘못 작업한 UX 개편(TKT-007)·gateway Dockerfile·redis.conf 는 `claude/ux-overhaul-stale-base-2026-08-16` 브랜치에 보존했다 — **병합 금지, 참고용** (트렁크에는 해당 파일들이 이미 존재).
- **왜**: 실개발이 이 브랜치에 있는데(VERSION 0.6.0, +36커밋, 문서 한국어화, Work Manager·블로그·Taste/Discovery 라인) main 만 보고 진단하면 전부 헛돈다 — 실제로 헛돌았다.
- **남은 위험**: main 승격(병합) 시점 미정. 낡은 main 을 기준으로 만든 외부 프리뷰 아티팩트는 폐기 대상.

## [claude] 통합 PM 체제 문서화 — CLAUDE.md 신설 + AGENTS.md 개정 (D-001)

- **무엇을**: 트렁크 위에 CLAUDE.md(PM 역할·기준선·리뷰 방법)를 신설하고, AGENTS.md 상단에 체제 변경 고지를 추가했다 — Codex = 구현 전담, PM = Claude. 기존 오케스트레이터/디자이너 서술은 원문 보존(가드레일·문서 규칙은 계속 유효), PO의 에이전트 재정산 후 재개정 예정.
- **왜**: PO 지시. 프로젝트별 PM 분산 → 단일 창구(`../ASK.md`)로 통합.

## [claude] 확장·편입 정책 확정 — 모노레포 폴더 기본 (D-003·D-004) + 제안 2건 (D-005·D-006)

- **무엇을**: `docs/decisions.md` 신설. PO 질문 "확장할 때마다 repo를 파야 하나" → **아니다, 기본값은 이 모노레포 안의 폴더** (D-004 확정): 화면 = `frontend/src/modules/`, 백엔드 = `services/`, 정적 게임 = `services/arcade/`. 독립성은 repo 경계가 아니라 계약으로. repo 신설은 예외 3조건(외부 독자 배포/이질 툴체인/실험 단계)일 때만. 편입은 `git subtree`로 이력 보존, 원 레포는 아카이브. 티켓 `scope` 필드 도입. 추가로 D-005(정적 자산 서비스 계약 신설)·D-006(같은 오리진 경로 라우팅 + 서비스 매니페스트) 제안 등록 — PO 확인 대기.
- **왜**: 편입 3종(developer advisor·화이트채플·+1) 중 화이트채플은 순수 정적이라 현행 서비스 계약(`/health`)을 만족할 주체가 없다. TKT-028(게임 공간)의 선행 조건도 같은 계약을 요구하므로 D-005 하나로 둘이 같이 풀린다.
- **남은 위험**: 편입 3종의 세 번째가 미확정(`../ASK.md` Q-013 — newProject 하위 레포 중 하나라고만 확인). AirportTycoon(C#/Godot)이라면 웹 탑재는 Godot Web export 검증이 선행돼야 한다.

## [claude] 화이트채플 subtree 편입 시행 (D-005 시행 + D-007)

- **무엇을**: PO의 시작 승인에 따라 `bitter_sweet_testbed` 공용 브랜치를 `services/arcade/whitechapel/` 로 `git subtree add`(이력 보존, WC 커밋 40건이 모선 이력에 편입). 게임 무관 학습 폴더 3종 제거. 정적 자산 서비스 계약을 `service-policy.md` 에 신설(D-005 확정·시행)하고 서비스 README 를 계약 표로 재작성. 흡수된 AGENTS.md/CLAUDE.md 에 편입 고지 추가 — **원 레포의 공용 브랜치·커밋 락 규칙 폐기**, WC 티켓·교차 리뷰·scope 는 폴더 안에서 유지. TKT-078(라우팅·매니페스트, v0.7.0) 발행.
- **검증**: 새 위치에서 `npm test` **37/37 통과**, `npm run build` → standalone.html 98,683B 생성.
- **왜**: D-004(모노레포 폴더 확장)의 첫 실행 사례. TKT-028 게임 공간의 첫 입주자를 확보해 정적 자산 계약의 실효성을 증명.
- **남은 위험**: ①흡수분에 미리뷰 WC-212·213(잭 플레이 모드·지도 가시성) 포함 — 화이트채플 보드에 미등재 상태라 교차 리뷰 필요 ②원 레포 아카이브는 PO 소관, 그 전까지 원 레포 신규 커밋 금지(두 집 살림 방지) ③advisor 편입은 이전 PO 지시("냅둬")와의 정합 확인 후 시행.

## [claude] advisor subtree 편입 시행 (D-008) — 디자인 차이는 백로그로

- **무엇을**: PO 승인("흡수하고 디자인이 다른 건 백로그로")으로 `~/Project/developerAdvisor` main 을 `services/advisor/` 로 subtree 흡수(dev 이력 23건 보존). 새 위치에서 `service/run.sh test` **통과**. 편입 고지 삽입(브랜치·PR 규칙 폐기, collab 2레인·콘텐츠 불가침 유지). 백로그 2장 발행 — **TKT-079**(디자인·프레임 통합, TKT-071 디자인 토큰 뒤 권장) · **TKT-080**(서비스 계약 마감: `/health` 부재 확인, compose 통합, CORS 재검토, 빌드 게이트 node 요구 명시).
- **왜**: D-004 모노레포 확장 원칙의 두 번째 사례. 기능 편입과 디자인 통합을 분리해 흡수 자체를 막지 않는다.
- **남은 위험**: ①frontend 빌드가 이 머신에서 비호환(vite 5.4↔node 20.10) — TKT-080 에서 환경 요구 명시로 해소 ②Anthropic API 직접 호출 구조의 키 관리·격리 예외 기록 필요(TKT-080) ③원 레포 아카이브·낡은 사본(`newProject/developer-advisor`) 삭제는 PO 소관.
