# 2026-08-16 작업 기록

## [claude] 통합 PM 체제 편입 — CLAUDE.md 신설

- **무엇을**: PO가 이 저장소를 `newProject/` 로 옮겨 통합 PM(Claude) 관리 대상에 넣었다. `CLAUDE.md` 를 신설해 Claude 레인(WC-2xx: 기획·UX·AI 정책·밸런스·배포)의 지침과 기존 운영 룰(`docs/works/README.md`)을 연결했다. 히스토리 규약을 `docs/history/YYYY-MM-DD.md` 로 정하고 이 파일을 첫 기록으로 남긴다.
- **왜**: 프로젝트마다 PM이 흩어져 PO의 질문 창구가 분산됐다. 상위 통합 규칙은 `../CLAUDE.md`, PO 질문은 `../ASK.md` 로 모은다.
- **기존 체계는 그대로 둔다**: 공용 브랜치(`claude/whitechapel-game-dev-4b46sk`) + `status: IN_PROGRESS` 커밋 락 + **교차 리뷰로만 DONE 전환** 구조는 다른 저장소의 보드+리뷰형보다 나은 장치라 판단해 통일하지 않았다. 통합 지점(ASK/BOARD)만 얇게 연결했다.
- **남은 위험**: 없음 — 코드·티켓 무변경, 문서 추가만.

## [claude] 현황 파악

- **상태**: 79커밋, 마지막 활동 2026-08-09. `WC-101~108`(codex 레인) · `WC-201~211`(claude 레인) 중 **`WC-205`(악몽 난이도 — Sonnet 연동 설계, P3) 하나만 TODO**, 나머지 전부 DONE.
- **구성**: `whitechapel/`(게임 UI·규칙 엔진·잭 AI) · `sim/`(헤드리스 시뮬레이션·MCTS 비교·벤치마크) · `tests/`(node --test) · `e2e/`(Playwright) · GitHub Pages 배포 워크플로.
- **통합 계획**: `workaround.co.kr-platform` 에 서브서비스로 편입 예정. **현재 GitHub Pages 정적 배포라 편입 시 배포 경로가 바뀐다** — 모선의 서비스 계약(자체 Dockerfile·`/health`)을 어떻게 만족시킬지가 편입 설계의 핵심 쟁점이다. 순수 정적 사이트라 `/health` 를 줄 주체가 없다는 점을 먼저 풀어야 한다.
