# 2026-06-14 인프라

## 요약

`TKT-027` 로 오케스트레이터 `need_review` heartbeat 를 저장소 안에서 바로 실행 가능한 로컬 스크립트로 구현했고, 관련 티켓/정책/워커 문서를 실제 상태에 맞게 갱신했다.

## 수행한 일

- `workers/orchestrator-heartbeat/heartbeat.py` 를 추가해 아래 문서를 읽는 10분 기본 루프와 `--once` 단발 모드를 구현했다.
  - `docs/tickets/need_review/`
  - `docs/tickets/board.md`
  - `docs/roadmap.md`
  - 최신 `docs/history/*.md`
- heartbeat 출력은 문서 자동 수정 대신 JSON 리포트(`needReviewTickets`, `activeBacklogUpToV030`, `latestHistory`, `nextAction`) 로 제한했다.
- `workers/orchestrator-heartbeat/README.md`, `workers/README.md`, `README.md` 에 실행 위치와 사용 방법을 추가했다.
- `docs/ticket-policy.md`, `docs/tickets/orchestrator.md` 를 “구현 없음” 상태에서 “로컬 실행 가능한 heartbeat 구현 있음” 상태로 갱신했다.
- `TKT-027` 을 `backlog` 에서 `need_review` 로 옮기고, 보드/인덱스 문서를 함께 맞췄다.
- Codex 앱의 현재 스레드에도 10분 heartbeat 자동화를 별도로 활성화해, 저장소 로컬 루프와 스레드 후속 확인을 함께 유지할 수 있게 했다.

## 검증

- `C:\Program Files\LibreOffice\program\python.exe -m py_compile workers/orchestrator-heartbeat/heartbeat.py`
- `C:\Program Files\LibreOffice\program\python.exe workers/orchestrator-heartbeat/heartbeat.py --once`

## 환경 메모

- 이 작업 환경에는 `python`, `py` 가 PATH 에 없었다.
- 로컬 검증은 `C:\Program Files\LibreOffice\program\python.exe` 로 수행했다.

## 남은 제약

- Windows 작업 스케줄러 등록, 서비스화, 부팅 시 자동 시작은 사용자 승인 전 수행하지 않았다.
- heartbeat 는 상태 감지와 요약 리포트까지만 수행하며, 자동 PR 생성이나 자동 문서 수정은 포함하지 않는다.

## `TKT-027` 추가 보강

- `workers/orchestrator-heartbeat/heartbeat.py` 가 이제 `docs/tickets/started/` 도 함께 읽고, `started` 티켓 안의 미완료 체크리스트를 `startedGateBlockers` 로 요약하도록 확장했다.
- `nextAction` 우선순위를 조정해 `need_review` 가 있더라도 진행 중 릴리스 게이트 막힘이 먼저 보이도록 맞췄다.
- `workers/orchestrator-heartbeat/README.md`, `workers/README.md`, `docs/ticket-policy.md`, `docs/tickets/orchestrator.md`, `docs/tickets/need_review/TKT-027-infra-orchestrator-heartbeat-automation.md` 를 같은 계약으로 갱신했다.

## `TKT-027` 추가 검증

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile workers/orchestrator-heartbeat/heartbeat.py`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe workers/orchestrator-heartbeat/heartbeat.py --once`

## `TKT-027` 추가 보강 2

- `workers/orchestrator-heartbeat/heartbeat.py` 가 이제 로컬 Git 상태도 함께 수집해 `gitStatus` 필드로 JSON 리포트에 포함한다.
- `gitStatus` 는 Git 실행 파일 경로, 버전, 현재 브랜치, 추적 브랜치, ahead/behind, dirty 여부, 변경 수 요약, 샘플 경로를 담아 `TKT-018` 의 Git branch/diff blocker 를 10분 주기 보고에 직접 드러낸다.
- `workers/orchestrator-heartbeat/README.md`, `docs/ticket-policy.md`, `docs/tickets/orchestrator.md`, `docs/tickets/need_review/TKT-027-infra-orchestrator-heartbeat-automation.md` 를 같은 출력 계약으로 갱신했다.
- `TKT-018` 에서는 “로컬 Git branch/diff 상태 확인” 체크를 완료 처리했고, `TKT-019` 에는 현재 `main...origin/main [behind 30]` + dirty 상태를 추가 기록했다.

## `TKT-027` 추가 검증 2

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile workers/orchestrator-heartbeat/heartbeat.py`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe workers/orchestrator-heartbeat/heartbeat.py --once`

## `TKT-027` 추가 보강 3

- `workers/orchestrator-heartbeat/run-heartbeat.ps1` 를 추가해 `python` PATH 가 없는 환경에서도 heartbeat 를 바로 시작할 수 있는 PowerShell 진입점을 만들었다.
- 이 스크립트는 Codex 번들 Python, LibreOffice Python, PATH 의 `python` 순서로 실행 파일을 찾아 `heartbeat.py` 에 `--once`, `--interval-seconds`, `--log-path`, `--repo-root` 인자를 넘긴다.
- `workers/orchestrator-heartbeat/README.md`, `workers/README.md`, `docs/tickets/need_review/TKT-027-infra-orchestrator-heartbeat-automation.md` 에 기본 실행 명령을 wrapper 기준으로 갱신했다.
- 운영 메모에 같은 콘솔에서 `Ctrl+C` 로 중지한다는 점도 함께 남겼다.

## `TKT-027` 추가 검증 3

- `powershell -ExecutionPolicy Bypass -File workers/orchestrator-heartbeat/run-heartbeat.ps1 -Once`

## `TKT-027` 추가 보강 4

- `workers/orchestrator-heartbeat/heartbeat.py` 가 이제 `toolchainStatus` 필드로 `java`, `mvn`, `docker` 가용성과 버전/오류도 함께 보고한다.
- `nextAction` 도 `gateway` Java/Maven compile, Docker build blocker 를 만났을 때 현재 도구 부재 여부를 함께 붙여 더 직접적인 운영 힌트를 주도록 보강했다.
- `workers/orchestrator-heartbeat/README.md`, `docs/ticket-policy.md`, `docs/tickets/orchestrator.md`, `docs/tickets/need_review/TKT-027-infra-orchestrator-heartbeat-automation.md` 를 같은 출력 계약으로 갱신했다.

## `TKT-027` 추가 검증 4

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile workers/orchestrator-heartbeat/heartbeat.py`
- `powershell -ExecutionPolicy Bypass -File workers/orchestrator-heartbeat/run-heartbeat.ps1 -Once`

## `TKT-027` 추가 보강 5

- `workers/orchestrator-heartbeat/heartbeat.py` 에 `blockingIssues` 요약층을 추가해 `startedGateBlockers`, `toolchainStatus`, `gitStatus` 를 사람이 바로 읽을 수 있는 막힘 목록으로 재구성했다.
- `gateway` Java/Maven compile blocker 는 `java unavailable`, `mvn unavailable` 같은 직접 원인을 붙이고, 로컬 작업 트리가 dirty 인 경우 환경 이슈도 별도 항목으로 함께 남긴다.
- `workers/orchestrator-heartbeat/README.md`, `docs/ticket-policy.md`, `docs/tickets/orchestrator.md`, `docs/tickets/need_review/TKT-027-infra-orchestrator-heartbeat-automation.md` 를 같은 출력 계약으로 갱신했다.

## `TKT-027` 추가 검증 5

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile workers/orchestrator-heartbeat/heartbeat.py`
- `powershell -ExecutionPolicy Bypass -File workers/orchestrator-heartbeat/run-heartbeat.ps1 -Once`

## `TKT-027` 추가 보강 6

- `workers/orchestrator-heartbeat/heartbeat.py` 가 이제 `nextCommands` 배열을 함께 출력해, 현재 막힘을 재확인하거나 다음 검증으로 이어갈 PowerShell 명령과 실행 가능 여부를 바로 제시한다.
- `gateway` Java/Maven compile, Docker build, Git status, heartbeat 단발 점검 명령을 상황별로 묶어 주고, 실행 불가한 경우에는 `java executable not found` 같은 이유를 함께 남긴다.
- `workers/orchestrator-heartbeat/README.md`, `docs/ticket-policy.md`, `docs/tickets/orchestrator.md`, `docs/tickets/need_review/TKT-027-infra-orchestrator-heartbeat-automation.md` 를 같은 출력 계약 기준으로 갱신했다.

## `TKT-027` 추가 검증 6

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile workers/orchestrator-heartbeat/heartbeat.py`
- `powershell -ExecutionPolicy Bypass -File workers/orchestrator-heartbeat/run-heartbeat.ps1 -Once`
  - `nextCommands` 에 `java -version`, `mvn -version`, `Set-Location gateway; mvn -q -DskipTests package`, `docker --version`, `docker build -t workaround-gateway:local gateway`, `& 'C:\Program Files\Git\cmd\git.exe' status --short --branch`, heartbeat 단발 명령이 포함됨을 확인했다.
  - 현재 환경에서는 `java`, `mvn`, `docker` 항목이 `runnable=false`, `git status` 와 `heartbeat once` 는 `runnable=true` 로 보고된다.

## `TKT-006` 추가 구현

- `workers/ion2-worker/worker.py` 를 `gateway-polling` 과 `redis-streams` 두 런타임 모드를 함께 지원하도록 확장했다.
- `workers/ion2-worker/redis_stream.py` 를 추가해 표준 라이브러리만으로 `XGROUP`, `XREADGROUP`, `XADD`, `XACK` 를 다루는 최소 Redis Streams 클라이언트를 넣었다.
- `workers/ion2-worker/enqueue_mock_ticket.py` 를 추가해 mock ticket 을 `platform:tickets` stream 으로 넣을 수 있게 했다.
- `workers/ion2-worker/smoke_redis_stream.py` 를 추가해 가짜 Redis/HTTP 서버 위에서 `enqueue_mock_ticket.py` 와 `worker.py` 를 실제 프로세스로 실행하는 end-to-end smoke 절차를 저장소 안에 남겼다.
- `infra/docker-compose.yml` 에 `ion2-worker` 의 Redis 의존성과 Redis Streams 관련 환경 변수를 추가하고, `elevator-service` 기본 층/대수 설정을 23층 4대로 맞췄다.
- `infra/.env.example`, `infra/README.md`, `infra/redis/README.md`, `workers/README.md`, `workers/ion2-worker/README.md`, `docs/ticket-policy.md` 를 새 런타임 모드와 smoke 절차 기준으로 갱신했다.
- `TKT-006` 을 `backlog` 에서 `need_review` 로 옮기고 보드/인덱스 문서를 함께 맞췄다.

## `TKT-006` 검증

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile workers/ion2-worker/worker.py workers/ion2-worker/redis_stream.py workers/ion2-worker/enqueue_mock_ticket.py workers/ion2-worker/smoke_redis_stream.py`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe workers/ion2-worker/smoke_redis_stream.py`

## `TKT-006` 남은 제약

- 실제 Docker Compose 전체 기동과 실 Redis 컨테이너 smoke 는 현재 작업 환경의 `docker` 부재 때문에 아직 검증하지 못했다.
- gateway 는 여전히 인메모리 ticket API 를 기본으로 유지하며, Redis Streams 발행 경로는 후속 인프라/게이트웨이 작업에서 연결해야 한다.

## `TKT-009` 추가 구현

- `workers/ion2-worker/worker.py` 에 Ollama 헬스 probe 와 LLM 티켓 판별 로직을 추가했다.
- worker 는 `type=job.llm.*`, `payload.requiresLlm=true`, `payload.inferenceProvider=ollama`, `payload.targetRuntime=ollama` 를 LLM 의존 티켓 신호로 본다.
- `OLLAMA_BASE_URL`, `OLLAMA_HEALTH_PATH`, `OLLAMA_TIMEOUT_SECONDS`, `OLLAMA_RETRY_AFTER_SECONDS` 환경 변수를 추가해 health path, timeout, retry 힌트를 조절할 수 있게 했다.
- Ollama 가 `ok` 가 아니면 gateway polling 모드에서는 티켓을 `waiting_llm` 으로 보내고 `retryNotBefore` 시각을 함께 남기도록 했다.
- `waiting_llm` 티켓은 `retryNotBefore` 시각이 지난 뒤에만 다시 집도록 worker polling 경로를 확장했다.
- Redis Streams 경로에서는 LLM 티켓이 Ollama unavailable / degraded 이면 결과 stream 에 `waiting_llm` 결과를 기록하고 원본 entry 를 ack 하도록 정리했다.
- `gateway/src/main/java/com/workaround/platform/gateway/PlatformGatewayApplication.java` 에 `POST /api/tickets/{ticketId}/waiting-llm` endpoint 와 `waiting_llm` 상태 저장 경로를 추가했다.
- `gateway` 헬스 응답의 ticket 집계에 `waiting_llm`, `retrying` 카운트를 추가했다.
- `workers/ion2-worker/smoke_ollama_degraded.py` 를 추가해 Ollama `ok`, `response_error`, `timeout`, `connection_failed` 분류와 “비LLM 티켓은 completed, LLM 티켓은 waiting_llm” 흐름을 함께 재현할 수 있게 했다.
- `docs/ollama-policy.md`, `docs/agent-runtime.md`, `docs/ticket-policy.md`, `workers/ion2-worker/README.md`, `gateway/README.md`, `infra/.env.example`, `infra/README.md` 를 새 degraded 흐름 기준으로 갱신했다.
- `TKT-009` 을 `backlog` 에서 `need_review` 로 옮기고 보드/인덱스 문서를 함께 맞췄다.

## `TKT-009` 검증

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile workers/ion2-worker/worker.py workers/ion2-worker/enqueue_mock_ticket.py workers/ion2-worker/redis_stream.py workers/ion2-worker/smoke_redis_stream.py workers/ion2-worker/smoke_ollama_degraded.py`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe workers/ion2-worker/smoke_ollama_degraded.py`
- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe workers/ion2-worker/smoke_redis_stream.py`

## `TKT-009` 남은 제약

- 현재 환경에는 `java`, `mvn`, `docker` 가 없어 gateway compile, gateway Docker build, 실제 외부 Ollama 연결 smoke 는 아직 검증하지 못했다.
- Redis Streams `waiting_llm` 결과는 현재 result stream 까지만 남기며, stream 재주입/자동 resume 전략은 후속 티켓에서 정리해야 한다.

## `TKT-004` 추가 구현

- `gateway/src/main/java/com/workaround/platform/gateway/PlatformGatewayApplication.java` 에 `GET /api/runtime` endpoint 를 추가해 노드 타깃, offload 규칙, 티켓 생성 예시, Ollama 상태 표현을 한 번에 노출하는 최소 runtime descriptor 를 넣었다.
- runtime descriptor 는 `ion2` 와 `rtx5070` 노드 역할, `service.loadProfile=light`, `ticket.targetNode=rtx5070`, `job.llm.*` 기준 routing rule, `waiting_llm` fallback 기준을 함께 반환한다.
- `frontend/src/App.vue` 는 `/api/runtime` 를 읽어 `Runtime targets` 패널과 rail 안내 문구에 offload 노드/규칙을 반영하도록 확장했다.
- `frontend/src/styles.css` 에 runtime panel, node availability badge, routing rule 스타일을 추가했다.
- `docs/architecture.md`, `docs/agent-runtime.md`, `docs/ollama-policy.md`, `docs/network.md`, `docs/frontend-ux.md`, `gateway/README.md` 를 새 runtime descriptor 와 책임 경계 표 기준으로 갱신했다.
- `TKT-004` 를 `backlog` 에서 `need_review` 로 옮기고 보드/인덱스 문서를 함께 맞췄다.

## `TKT-004` 검증

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe node_modules/vite/bin/vite.js build --configLoader runner`

## `TKT-004` 남은 제약

- 현재 환경에는 `java`, `mvn`, `docker` 가 없어 gateway compile, gateway Docker build, 실제 `/api/runtime` 서버 응답 smoke 는 아직 검증하지 못했다.
- runtime descriptor 는 정책과 UI 표현 기준선에 집중한 최소 구현이며, 실제 RTX5070 호스트 세부 사양/배포 절차는 `TKT-007`, `TKT-008` 에서 이어서 구체화해야 한다.

## `TKT-007` 추가 구현

- `infra/rtx5070-host/README.md` 를 추가해 RTX5070 호스트 준비 절차, Windows/WSL2 기본 경로, Docker 자동 시작, GPU smoke 증거 수집 기준을 정리했다.
- `infra/rtx5070-host/preflight.ps1` 를 추가해 host OS, GPU, `nvidia-smi`, WSL2, Docker CLI, Docker Compose, Docker 서비스, 선택적 `docker run --gpus all ... nvidia-smi` smoke 를 JSON 리포트로 점검할 수 있게 했다.
- `infra/docker-compose.gpu.yml` 에 `restart: unless-stopped` 와 `ollama list` 기반 healthcheck 를 추가했다.
- `infra/README.md`, `llm/ollama/README.md`, `docs/ollama-policy.md` 를 새 host preflight 경로 기준으로 갱신했다.
- `TKT-007` 을 `backlog` 에서 `need_review` 로 옮기고 보드/인덱스 문서를 함께 맞췄다.

## `TKT-007` 검증

- `powershell -ExecutionPolicy Bypass -Command "& { & 'C:\project\workaround.co.kr-platform\infra\rtx5070-host\preflight.ps1' -AsJson }"`
  - 현재 환경 기준 `nvidia-smi` 는 성공했고 RTX5070 메모리/드라이버 정보가 보였다.
  - `wsl --status` 도 성공했다.
  - 반면 `docker`, `docker compose`, `com.docker.service` 는 없거나 찾지 못했다.
  - `docker-gpu-smoke` 는 `-RunDockerGpuSmoke` 미사용 기준으로 `skipped` 처리됐다.

## `TKT-007` 남은 제약

- 실제 Docker 설치, Docker 서비스 자동 시작 설정, `docker run --gpus all ... nvidia-smi` 성공까지는 이 세션에서 수행하지 못했다.
- 따라서 이 작업은 “호스트 준비 절차와 preflight 기준선 제공 + 현재 호스트 상태 증거 수집” 까지를 완료 근거로 남기고, 실제 설치/승인 단계는 후속 검토 또는 사용자 승인 뒤 이어가야 한다.

## `TKT-008` 추가 구현

- `infra/rtx5070-host/ollama-runtime.ps1` 를 추가해 RTX5070 호스트에서 Ollama 컨테이너를 `start`, `stop`, `restart`, `pull-models`, `health`, `logs`, `status` 로 제어하는 표준 PowerShell 진입점을 만들었다.
- `ollama-runtime.ps1` 는 `infra/docker-compose.gpu.yml` 을 기준 compose 로 사용하고, `/api/tags` 헬스를 `ok`, `degraded`, `unavailable` 로 분류해 워커가 소비하는 상태 표현과 맞추도록 했다.
- `infra/rtx5070-host/README.md`, `infra/README.md`, `llm/ollama/README.md`, `docs/ollama-policy.md`, `docs/network.md` 를 갱신해 아래 내용을 문서화했다.
  - 호스트 직접 점검 URL: `http://localhost:11434`
  - 같은 PC 안의 컨테이너 기본 URL: `http://host.docker.internal:11434`
  - 별도 RTX5070 장비일 때의 대체 URL: `http://rtx5070-host:11434`
  - 첫 모델 후보: `qwen2.5-coder:7b`, `qwen2.5:7b`
  - GPU 경합 전 중지 절차와 health JSON 예시
- `TKT-008` 을 `backlog` 에서 `need_review` 로 이동시키고 보드/인덱스 문서를 맞췄다.

## `TKT-008` 검증

- `powershell -ExecutionPolicy Bypass -Command "& { & 'C:\project\workaround.co.kr-platform\infra\rtx5070-host\ollama-runtime.ps1' -Action health -AsJson }"`
  - 현재 세션에서는 `http://localhost:11434/api/tags` 연결 실패를 `unavailable/connection_failed` 로 반환하는 것을 확인했다.
- `powershell -ExecutionPolicy Bypass -Command "& { & 'C:\project\workaround.co.kr-platform\infra\rtx5070-host\ollama-runtime.ps1' -Action status -AsJson }"`
  - 현재 세션에서는 `docker` 부재 때문에 compose 상태 확인이 실패하고, 다음 단계 힌트가 함께 반환되는 것을 확인했다.

## `TKT-008` 남은 제약

- 이 세션에는 여전히 `docker` 가 없어 실제 `docker compose up`, `ollama pull`, `/api/tags` 2xx 성공까지는 검증하지 못했다.
- 따라서 이번 작업은 “실제 운영 스크립트와 문서 계약 정리 + 부재 환경에서의 실패 표현 검증” 까지를 완료 근거로 남긴다.

## `TKT-014` 추가 구현

- `services/public-site/` 를 추가해 `workaround.co.kr` 공개 소개 페이지용 정적 서비스를 저장소 안에 만들었다.
  - `app.py`: `/`, `/styles.css`, `/site.webmanifest`, `/health` 를 제공하는 경량 Python HTTP 서비스
  - `Dockerfile`, `README.md`, 정적 HTML/CSS/manifest
- `infra/public-site/` 를 추가해 공개 서버 배포 기준선을 정리했다.
  - `docker-compose.public-site.yml`: `public-site` + `caddy`
  - `Caddyfile`: `workaround.co.kr` canonical, `workaround.kr`/`www.*` redirect, TLS 자동 발급 기준
  - `.env.public-site.example`, 운영 README
- `README.md`, `services/README.md`, `infra/README.md`, `infra/nginx/README.md`, `docs/architecture.md`, `docs/network.md`, `docs/hosting-options.md` 를 갱신해 공개 사이트와 내부 프리뷰의 역할 분리, public ingress, Caddy 기본 선택을 문서화했다.
- `docs/roadmap.md`, `docs/steering.md` 도 함께 갱신해 reverse proxy 선택 질문은 닫고, 남은 운영 질문을 “실서버 포트/접속/TLS 가능 여부” 쪽으로 좁혔다.
- `TKT-014` 를 `backlog` 에서 `need_review` 로 이동시키고 보드/인덱스 문서를 맞췄다.

## `TKT-014` 검증

- `C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m py_compile services/public-site/app.py`
- `public-site` 를 로컬 Python 프로세스로 띄운 뒤 아래 응답을 확인했다.
  - `GET /health` -> 200, `canonicalDomain=workaround.co.kr`, `aliasDomain=workaround.kr`
  - `GET /` -> 200, 소개 페이지 HTML 응답
  - `GET /site.webmanifest` -> 200

## `TKT-014` 남은 제약

- 현재 세션에는 `docker` 가 없어 `infra/public-site/docker-compose.public-site.yml` 실기동과 Caddy 기반 TLS/redirect 실제 동작은 검증하지 못했다.
- 실제 서버 OS, SSH 접근 방식, 공개 포트, DNS 관리 콘솔 정보가 아직 없어 “문서+배포 번들+정적 서비스 기준선” 까지를 완료 근거로 남긴다.
