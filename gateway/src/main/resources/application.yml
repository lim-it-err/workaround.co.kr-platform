server:
  port: 8080

spring:
  application:
    name: workaround-gateway

app:
  platform:
    api-key: ${PLATFORM_API_KEY:dev-key}
    elevator-service-url: ${ELEVATOR_SERVICE_URL:http://localhost:8003}
    sample-spring-service-url: ${SAMPLE_SPRING_SERVICE_URL:http://localhost:8002}
  work-manager:
    password-sha256: ${WORK_MANAGER_PASSWORD_SHA256:8e0894262a5710556e3a186ff6476306c62c8c91f9853249fb878bf4f2826176}
    session-ttl-minutes: ${WORK_MANAGER_SESSION_TTL_MINUTES:30}
    max-failed-attempts: ${WORK_MANAGER_MAX_FAILED_ATTEMPTS:5}
    lock-minutes: ${WORK_MANAGER_LOCK_MINUTES:5}
    trusted-proxies: ${WORK_MANAGER_TRUSTED_PROXIES:}
  ollama:
    base-url: ${OLLAMA_BASE_URL:http://rtx5070-host:11434}
