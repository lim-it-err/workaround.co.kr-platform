# LLM

LLM-related configuration and policy live here.

The MVP assumes Ollama runs outside the default compose stack on an RTX5070 node.

See `docs/ollama-policy.md` before changing LLM topology or availability behavior.
