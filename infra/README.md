# Infrastructure

Infrastructure configuration for local development and future deployment.

Expected future files include:

- `docker-compose.yml`
- Redis configuration
- Nginx configuration
- Optional GPU compose file for RTX5070 Ollama runtime

The MVP default should not require Ollama to be running locally.
