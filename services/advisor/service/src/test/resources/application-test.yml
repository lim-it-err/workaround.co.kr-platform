spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  datasource:
    url: jdbc:h2:mem:advisor-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

advisor:
  llm:
    roles:
      generate: { provider: mock, model: default }
      review: { provider: mock, model: default }
      chat: { provider: mock, model: default }
