# 에이전트 공통 안내

> **편입 고지 (2026-08-16, D-003·D-004)**: 이 폴더는 `bitter_sweet_testbed` 레포에서 workaround.co.kr-platform 모노레포의 `services/arcade/whitechapel/` 로 subtree 흡수됐다. **아래 "작업 브랜치" 규칙은 더 이상 적용되지 않는다** — 브랜치·커밋은 모선 규칙(루트 `CLAUDE.md`)을 따른다. WC-### 티켓 보드(`docs/works/`)와 교차 리뷰 원칙은 이 폴더 안에서 계속 유효하다. 수정 범위는 티켓의 `scope` 를 벗어나지 않는다 (D-004). 원 레포는 아카이브 대상.

이 폴더의 `whitechapel/` 게임 개발은 파일 기반 티켓 보드로 협업한다.

- 운영 룰: `docs/works/README.md`
- 현황판: `docs/works/BOARD.md`
